# 永续合约 DEX 综合研究报告

## 第一部分：执行摘要与方法论

**日期：** 2026-03-21

**测试钱包：** `0x41Deec4e76e46c18719E90920C1efef370B7DB0A`（全新 EVM 钱包，零真实资金）

**模式：** 尽可能使用测试网 / 零真实资金

**覆盖平台：** 13 个

1. Hyperliquid
2. Aster
3. trade[XYZ]
4. EdgeX
5. Variational
6. Lighter
7. GRVT
8. Extended
9. Paradex
10. Ethereal
11. Nado
12. StandX
13. MYX Finance

**研究方法：**

- 官方文档审查（GitBook、Notion、GitHub、API 文档）
- CSP（内容安全策略）头部提取（来自生产环境前端）
- 运行时 JS bundle 分析（供应商 SDK 检测、环境变量提取）
- 实时 API 探测（基于 curl，零认证和认证探测）
- 本地 SDK 探测（Python、Node.js），使用测试钱包
- OpenAPI/Swagger 规范分析（如有）
- 网络搜索团队、融资和产品背景
- DNS 解析检查，针对旧版/失效端点

---

## 第二部分：核心发现（系统性洞察）

### 1. 嵌入式钱包格局

Privy 出现在 **13 个平台中的 7 个**：

| 平台 | Privy | 其他嵌入式钱包基础设施 |
|---|---|---|
| Paradex | 是（CSP、文档） | Argent、Ready.co |
| Hyperliquid | 是（文档、支持） | -- |
| trade[XYZ] | 是（CSP） | -- |
| EdgeX | 是（CSP、JS bundle） | -- |
| GRVT | 是（邮箱 OTP 层） | Dfns（MPC/WebAuthn） |
| Ethereal | 是（CSP frame-src） | Fun.xyz（钱包 SDK）、MoonPay（法币） |
| Extended | 否 | Dynamic.xyz（JS bundle） |
| MYX Finance | 否 | Particle Network、Dynamic.xyz、Magic.link、Biconomy、Safe |
| Aster | 否 | 邮箱登录（原生） |
| StandX | 否 | 未发现 |
| Lighter | 否 | 未发现 |
| Nado | 否 | 未发现 |
| Variational | 否 | 未发现 |

**关键洞察：** 第一轮分析遗漏了 **Ethereal**、**Extended** 和 **EdgeX** 的嵌入式钱包基础设施，因为仅检查了官方文档。CSP 头部和 JS bundle 分析揭示了真相：

- **Ethereal：** CSP `frame-src` 中有 `auth.privy.io`，HTML 源码中有 `funkit`（Fun.xyz），`connect-src` 中有 `MoonPay` API —— 这些在开发者指南中均未记录。
- **Extended：** 1.77MB 的 JS bundle 中包含 `dynamicEnvironmentId` 和 `dynamicauth` —— Dynamic.xyz（Privy 的竞争对手）支持邮箱/社交/嵌入式钱包，但尚未在文档或 UI 中展示。
- **EdgeX：** CSP `script-src` 和 `connect-src` 中有 `auth.privy.io`，以及一个 `lib-privy` JS bundle —— 文档仅描述了通用的"MPC 邮箱"流程。

**GRVT 的双重基础设施**是最复杂的：Dfns 提供实际的 MPC 钱包层（WebAuthn 密钥、备份代码、凭据管理），而 Privy 处理邮箱 OTP 认证前端。测试网环境配置确认 `DFNS_APP_ID` 和 `PRIVY_APP_ID` 均处于活动状态，且 `DFNS_PRIVY_ENABLE=1`。

**MYX Finance** 拥有整个样本中最广泛的钱包基础设施栈：Particle Network + Biconomy + Dynamic.xyz + Magic.link + Safe + WalletConnect + ConnectKit + RainbowKit，并有 EIP-4337 和 EIP-7702 的账户抽象引用。

### 2. API 认证模型分类

所有 13 个平台按其认证模式分类：

**JWT / 会话 cookie：**
- **GRVT** —— EIP-712 钱包登录或 API key 登录，两者都返回会话 cookie。每个 Trading Account 最多 5 个密钥，每个密钥绑定一个 Ethereum 地址。
- **StandX** —— 通过钱包签名获取 JWT（SIWE + ed25519 密钥对）。写操作需要 Body 签名（ed25519）。

**API key + HMAC：**
- **Aster（Pro API）** —— `X-MBX-APIKEY` 头部 + HMAC SHA256 签名。每个账户最多 30 个 API key。标准的类 CEX 模式。

**API key + Stark 签名：**
- **Extended** —— 只读访问使用 API key（`X-Api-Key` 头部），写操作使用 API key + SNIP12/EIP-712 Stark 签名。每个子账户密钥独立，每个钱包最多 10 个账户。

**EIP-712 逐操作签名（无 API key）：**
- **Ethereal** —— 8 种不同的签名操作类型（LinkSigner、TradeOrder、CancelOrder、InitiateWithdraw 等）。无 JWT 或 API key 替代方案。49 个 REST 端点。
- **Nado** —— 纯钱包签名。无 API key，无用户名/密码。链接签名者可选（1-Click Trading）。

**原始钱包签名：**
- **Hyperliquid** —— 直接钱包签名或已批准的 API/代理钱包。代理钱包代表主账户签名。

**JWT 通过钱包签名 + Body 签名：**
- **StandX** —— 双层：JWT 用于会话认证，ed25519 Body 签名用于写操作。

**仅链上（无 REST 交易 API）：**
- **MYX Finance** —— 仅 2 个公共 REST 端点（市场数据）。所有交易通过智能合约调用（Router.sol）或 Seamless Key（链上委托签名者）。
- **Variational** —— 当前上线的 API 是只读的 Omni 统计数据。交易 API 明确表示"仍在开发中"。

**依赖上游：**
- **trade[XYZ]** —— 直接使用 Hyperliquid API。主网：`api.hyperliquid.xyz`，测试网：`api.hyperliquid-testnet.xyz`。XYZ 市场传递 `type: perpDexs`。

**多层（API key + 密码短语 + L2 签名）：**
- **EdgeX** —— 私有 API 网关强制要求 `X-edgeX-Api-Key` + `X-edgeX-Passphrase` + `X-edgeX-Signature` + `X-edgeX-Timestamp`。订单操作需要单独的 L2 私钥 / `l2Signature`（Pedersen 哈希 + ECDSA）。

**双轨制（直接 API + 委托构建者）：**
- **Aster** —— Pro API 用于直接交易者（HMAC），同时 Aster Code 用于构建者/代理委托（独立的 `/fapi/v3/` 端点，带有每用户签名者密钥）。

**多凭据（JWT + 子密钥 + 只读令牌）：**
- **Paradex** —— JWT 令牌，主私钥用于完全访问，子密钥用于仅交易，只读令牌用于长期 GET 访问。EVM SIWE v2 入门 + Starknet L2 密钥交易。

**先充值 + API key + 认证令牌 + nonce：**
- **Lighter** —— 通过入金创建账户。然后 API key（索引 2-254），生成认证令牌，每密钥 nonce 管理。Standard 与 Premium 层级有不同的速率限制。

### 3. 冷钱包摩擦度谱系

按全新钱包首次通过 API 下单的难易程度，对所有 13 个平台进行排名：

| 排名 | 平台 | 难度 | 阻碍因素 |
|---|---|---|---|
| 1 | Extended（测试网） | 低 | 入门 + API key + Stark 签名 —— 全部可脚本化，端到端已验证 |
| 2 | StandX | 中 | 钱包签名 + JWT + DUSD 充值 + 转入 Perps 钱包 |
| 3 | Hyperliquid | 中 | Enable Trading + 充值/激活 + 代理钱包设置 |
| 4 | Paradex | 中 | 最低链上余额（0.001 ETH 或 5 USDC）+ 入门 + L2 密钥推导 |
| 5 | Aster | 中 | 入门 + 充值 + UI 端 API key 创建 + HMAC 签名 |
| 6 | trade[XYZ] | 中 | Privy 简化了前端，但底层仍是 Hyperliquid 激活流程 |
| 7 | Lighter | 中高 | 充值 + 等待入账 + account_index + API key 关联 + 认证令牌 |
| 8 | Nado | 中高 | 首次充值创建子账户，然后每个操作都需要钱包签名 |
| 9 | Ethereal | 高 | Discord 请求测试网资金 + 充值 + 子账户 + EIP-712 逐操作签名 |
| 10 | EdgeX | 高 | API key + 密码短语 + 签名 + 时间戳 + 独立 L2 密钥 + l2Signature |
| 11 | GRVT | 高 | Web2 账户 + KYC + SecureKey + Trading Account + 充值 + API Key + 签名者标签 |
| 12 | MYX Finance | 不适用 | 无 REST 交易 API；必须直接调用智能合约 |
| 13 | Variational | 不适用 | 交易 API 尚未可用；仅有只读统计数据 |

### 4. Web2 与 API 优先的权衡

整个样本的根本分歧：平台优化其中一个方向，很少两者兼顾。

**Web2 优先平台**（最流畅的入门体验，API 初始化摩擦最高）：
- MYX Finance：Particle Network 社交登录 + Seamless Key + 信用卡购买 USDC —— 但零 REST 交易 API
- GRVT：邮箱 + Google + Apple + Microsoft OAuth + Dfns+Privy —— 但 API 需要 7 步初始化链
- trade[XYZ]：Privy 邮箱钱包被描述为"最快选项" —— 但继承了 Hyperliquid 的激活约束

**API 优先平台**（最快到达程序化交易，最少的 Web2 让步）：
- Extended：第一轮即完成了完整的写路径验证，Dynamic.xyz 在 bundle 中但未展示
- StandX：干净的类 CEX REST+WS API，但仅支持钱包登录
- Lighter：成熟的 API key + 认证令牌栈，但需先充值且无邮箱登录

**平衡型（尝试两者兼顾）：**
- Paradex：Privy 邮箱/社交 + JWT/子密钥 API 模型 —— 但冷钱包仍受限
- Hyperliquid：Privy 邮箱 + 代理钱包 —— 但写操作需要激活/充值
- Aster：邮箱登录 + 双轨 Pro API / Aster Code —— 仍需 UI 端创建 API key

---

## 第三部分：平台矩阵表

| 平台 | 登录/用户入门 | 嵌入式/AA 类路径 | API 认证与下单路径 | 全新钱包 -> API 首次下单 | 实证依据 |
| --- | --- | --- | --- | --- | --- |
| Hyperliquid | 官方文档支持普通钱包或邮箱登录 | 是；邮箱登录有明确支持，支持文档将其与 `Privy` 关联，且邮箱钱包可导出到普通钱包扩展 | 原始钱包签名或已批准的 API 钱包/代理钱包 | 从冷测试钱包来看为中低难度；公共/读路径可立即查询，但官方文档仍要求在签名写操作正常工作前完成 `Enable Trading` 以及资金充足/激活的 HyperCore 状态 | 主网 + 测试网 `info/meta` 正常运行；新钱包仍返回零值读取状态，但签名的 `noop` 和 `market_open` 再次失败，提示 `User or API Wallet does not exist`；官方文档说测试网水龙头需要同一地址的主网充值，且未激活账户无法发送 `CoreWriter` 操作 |
| Aster | 钱包登录和官方邮箱登录指南均存在 | 邮箱登录会创建与邮箱绑定的新区块链地址 | 两条私有路径：标准 Pro API 使用自助 API key + HMAC SHA256 进行直接交易，另有独立的 `Aster Code` 构建者/代理端点支持委托执行 | 中等难度；新钱包仍需入门、充值和 UI 端创建 API key，但直接交易者的访问比第一轮仅关注构建者的定位要简单得多 | `/fapi/v1/ping`、`/fapi/v1/time`、`/fapi/v1/exchangeInfo` 正常运行；官方 API 页面说每个账户可创建最多 30 个 API key；`Aster Code` `/fapi/v3/agent` 和 `/fapi/v3/builder` 仍然独立受签名保护 |
| trade[XYZ] | 现有钱包或 Privy 邮箱钱包 | Privy 邮箱钱包有明确支持 | 直接使用 Hyperliquid API 访问 XYZ 市场，包括 XYZ 资产的 `type: perpDexs` 路由 | 中等难度；Privy 简化了入门流程，但连接钱包的用户仍需充值，有时还需 `Enable Trading`，因此底层同样受 Hyperliquid 激活/充值约束 | 首页正常运行；CSP connect-src 暴露了 `auth.privy.io` 和 `api-ui.hyperliquid.xyz`；官方 API 文档直接指向 Hyperliquid 主网/测试网端点 |
| EdgeX | 标准钱包或邮箱 MPC 登录 | 基于邮箱的 MPC 钱包有明确支持 | 双层认证栈：私有 REST 认证头部加上独立的 L2 私钥 / `l2Signature` 用于下单操作 | 从原始钱包单独操作难度较高；需要专门设置 | 公共资金费率端点正常运行；私有 API 网关当前强制要求 `Api-Key -> Passphrase -> Signature`，甚至 `registerAccount` 也在同一私有头部门控之后 |
| Variational | 钱包优先的 Omni 流程；当前上线的应用是 Omni，Pro 尚未上线 | 未发现嵌入式钱包证据；无 gas 充值减少了摩擦但并未创建嵌入式钱包路径 | 当前上线的 API 是只读的 Omni 统计数据；交易 API 仍不可用；旧版 Pro/API-key 文档和主机现在看起来已归档 | 目前 `全新钱包 -> API 首次下单` 实际上不可用 | Omni 主网/测试网应用和统计端点正常运行；当前文档说交易 API 仍在开发中且 Pro 尚未上线；旧版认证/设置文档 URL 现在返回 404，旧的 `api.*` / `pro.testnet.*` 主机名无法解析 |
| Lighter | Ethereum 钱包加充值支持的账户创建 | 未发现嵌入式钱包证据 | 每账户/子账户 API key + 认证令牌 + nonce 模型 | 低难度；程序化账户创建首先需要入金到账，然后查找 account index，再设置 API key | `orderBookDetails` 正常运行；钱包查询返回 `account not found`，但 `createIntentAddress` 已为同一新钱包工作，确认充值是真正的第一个程序化步骤 |
| GRVT | 明确的 `Web2 账户 + SecureKey + 账户结构` 模型；Web2 登录包括邮箱 + Google + Apple + Microsoft OAuth | 样本中最强的嵌入式路径：GRVT Native SecureKey 由 `Dfns`（MPC/WebAuthn/密钥/备份代码）+ `Privy`（邮箱 OTP 认证层）提供支持 —— 这是一个双重基础设施组合，而非仅 Privy | 通过 API key 或 EIP-712 钱包登录的会话 cookie 认证；每个 Funding/Trading Account 最多 5 个密钥；每个密钥绑定到一个 Ethereum 地址 | 从全新钱包来看难度较低；仅在注册 SecureKey 加上 funding/trading-account 初始化后变为中等难度；KYC 现已成为必需（`NEW_KYC_ENABLE=1`） | 实时钱包登录仍返回 `401 wallet address not registered`；测试网环境配置确认 `DFNS_PRIVY_ENABLE=1`，`PRIVY_APP_ID` 和 `DFNS_APP_ID` 均处于活动状态；主网市场数据正常运行（95 种工具）；官方 API 设置指南要求 `创建 Web2 账户 -> SecureKey -> Web3 账户 -> 充值 -> API Key` |
| Extended | 文档中的钱包优先账户创建；JS bundle 包含 `Dynamic.xyz` 钱包 SDK（支持邮箱/社交/嵌入式钱包）但尚未可见展示 | Dynamic.xyz（`dynamicEnvironmentId`）存在于 JS bundle 中 —— 不是 Privy；第一轮说"未发现嵌入式钱包证据"是错误的 | 读取使用 API key + 写入使用 API key + Stark 签名（SNIP12/EIP-712）；每个子账户独立；每个钱包最多 10 个账户 | 测试网上难度较高（第一轮已完成完整写路径验证）；主网需要真实 USDC 充值；121 个主网市场，31 个测试网 | 主网正常运行，BTC 日交易量 $339M；`Rhino.fi` 用于跨链桥接（Arbitrum）；SDK `drop-api-keys` 分支暗示未来认证简化 |
| Paradex | 当前文档支持直接 EVM 入门和 Privy 支持的邮箱/社交钱包；前端入门比旧版仅钱包叙事更友好 | 是；当前文档明确支持 Privy 邮箱/社交钱包，实时应用/测试网 CSP 确认 Privy 运行时 | JWT + 主私钥/子密钥/只读令牌；EVM SIWE v2 入门/认证加上 Starknet 子密钥交易，而官方 SDK/代码示例仍倾向于旧版 `/onboarding` + `/auth` + L2 私钥流程 | 中等难度；前端入门更流畅，但全新原始钱包在账户创建/初始化之前仍无法干净地跳转到程序化交易 | 主网/测试网应用正常运行并在 CSP 中暴露 Privy；测试网配置正常运行；本地 `/v1/onboarding` 探测命中 `INSUFFICIENT_MIN_CHAIN_BALANCE`，然后 `/v1/auth` 返回 `NOT_ONBOARDED`；文档说新测试网账户创建后会自动充值 |
| Ethereal | 文档/API 中钱包优先入门，但 CSP 揭示了 `Privy` + `Fun.xyz` + `MoonPay` 集成在实时前端中 | CSP 的 frame-src 包含 `auth.privy.io`，HTML 中有 `funkit`（Fun.xyz SDK）—— 嵌入式钱包基础设施存在但尚未在开发者指南中记录 | EIP-712 逐操作签名（8 种类型），无 API key 替代方案；49 个 REST 端点 + 原生 WebSocket v2；链接签名者用于委托 | 从原始钱包来看难度较低；首次充值（最低 10 USDe）到子账户是强制性的；测试网资金现在需要通过 Discord 请求（无公共水龙头） | 15 个主网 / 17 个测试网产品正常运行；钱包仍返回空的子账户 + `404 Signer not found`；CSP 还显示 Relay、Merkl、Zerion、Binance Wallet 和 ZK Lighter 交叉引用 |
| Nado | 钱包优先，非常明确的 CEX 转 DEX 入门文档 | 未发现嵌入式钱包证据；链接签名者支持 1-Click Trading 但仍为可选 | 无 API key；仅钱包签名；链接签名者可选 | 从原始钱包来看难度较低；首次充值创建子账户，但在子账户存在之前已可查询到确定性默认子账户的直接充值地址 | Gateway 状态活跃；默认子账户 `exists=false`；链接签名者为零地址；archive 已为该同一默认子账户返回充值地址 |
| StandX | 钱包优先（BSC + Solana）；无邮箱/社交登录可见；CSP 或 HTML 中无嵌入式钱包信号 | 未发现嵌入式钱包证据 | 通过钱包签名获取 JWT（SIWE + ed25519 密钥对），无传统 API key；自托管令牌管理，可配置权限/过期时间；写操作需要 Body 签名（ed25519） | 中高难度；完整的 REST + WebSocket 交易 API，但必须持有 DUSD（专有稳定币）并使用双钱包系统（Cash + Perps） | API 在 `perps.standx.com` 正常运行；BTC-USD 24 小时交易量 $297M；prepare-signin 为测试钱包返回有效 JWT；私有端点返回 `401 missing jwt`；4 个市场（BTC、ETH、XAU、XAG） |
| MYX Finance | 双路径：钱包连接或通过 `Particle Network` 的社交登录（邮箱/手机/社交） | 是；`Particle Network` 用于社交登录 + 嵌入式钱包，`Biconomy` 用于无 gas AA，`Seamless Key` 作为委托链上签名者；JS bundle 还包含 Dynamic.xyz、Magic.link、Safe、ConnectKit、RainbowKit | 仅 2 个公共 REST 端点（市场数据）；所有交易通过智能合约或 Seamless Key 在链上进行 —— 无链下 REST 交易 API | API 优先难度较低；无 REST 下单 API；必须直接与智能合约交互或使用 Seamless Key（链上委托签名者） | `api.myx.finance` 返回 37 个永续合约交易对；JS bundle 确认 Particle + Biconomy + WalletConnect + Dynamic + Magic + Safe；V2 具有 EIP-4337/7702 由 Consensys 资助但尚未上线 |

---

## 第四部分：详细平台分析

### 4.1 Hyperliquid

**概述与入门：**

- 官方入门文档说用户可以使用 `normal defi wallet` 或通过 `logging in with your email address` 进行交易。
- Hyperliquid 的邮箱路径不是模糊的便利封装：
  - 入门文档说邮箱登录会创建 `a new blockchain address`
  - 支持文档明确提到 `Privy`
  - 文档还允许用户 `Export Email Wallet` 并将私钥复制到普通钱包扩展中
- 对于连接钱包的入门，官方流程不仅仅是 `connect and go`：用户被告知要点击 `Enable Trading` 并签署一笔无 gas 交易，然后再充值。
- 测试网水龙头文档明确指出 `email login` 在主网和测试网创建不同的钱包地址，且用户可以 `export the email wallet`。
- 同一份测试网水龙头文档说水龙头访问不向任意新地址开放：你必须先用 `deposited on mainnet with the same address`。
- 在 API 方面，Hyperliquid 支持由主账户批准的 `API wallets` / `agent wallets`。
- 开发者文档还说新的 HyperCore 账户在首次入站交易时会产生一次性 `1 quote token` 激活费，且 `unactivated accounts cannot send CoreWriter actions`。
- 邮箱入门还为开发者带来了一个微妙的操作陷阱：文档明确说 `Privy` 在主网和测试网生成不同的钱包地址，因此邮箱用户不会自动以相同身份满足水龙头要求，除非他们导出/导入钱包。

**API 认证：**

- 原始钱包签名或已批准的 API/代理钱包
- 代理钱包代表主账户或子账户签名
- 没有传统意义上的 API key

**实时探测结果：**

- `POST https://api.hyperliquid.xyz/info {"type":"meta"}` 返回了主网 universe 数据。
- `POST https://api.hyperliquid-testnet.xyz/info {"type":"meta"}` 返回了测试网 universe 数据（207 种工具）。
- `POST https://api.hyperliquid-testnet.xyz/info {"type":"clearinghouseState","user":"<wallet>"}` 返回了有效的零值账户状态。
- `POST https://api.hyperliquid-testnet.xyz/info {"type":"openOrders","user":"<wallet>"}` 返回 `[]`。
- `POST https://api.hyperliquid-testnet.xyz/info {"type":"subAccounts","user":"<wallet>"}` 返回 `null`。
- 签名的 `noop` 操作返回：`User or API Wallet 0x41deec4e76e46c18719e90920c1efef370b7db0a does not exist.`
- 签名的 `market_open("BTC", True, 0.001, ...)` 返回了同样的 `User or API Wallet ... does not exist` 错误。

**第二轮修正：**

- 邮箱入门是一条明确的 Privy 支持的嵌入式路径，带有钱包导出。
- 写路径仍然依赖于 Enable Trading、资金充足/激活的 HyperCore 状态和测试网水龙头门控。
- 主网和测试网 meta 均确认正常运行。

**评估：**

- `Web2 友好度`：中高
- `API 下单可用性`：激活后高，从冷原始钱包来看中低
- `关键摩擦`：`Enable Trading` + 充值/激活 + 水龙头门控都阻挡在签名测试网写路径验证之前；`Privy` 邮箱钱包的主网/测试网拆分仍然是一个微妙的陷阱

**来源：**

- https://hyperliquid.gitbook.io/hyperliquid-docs/onboarding/how-to-start-trading
- https://hyperliquid.gitbook.io/hyperliquid-docs/onboarding/testnet-faucet
- https://hyperliquid.gitbook.io/hyperliquid-docs/onboarding/export-your-email-wallet
- https://hyperliquid.gitbook.io/hyperliquid-docs/support/faq/connectivity-issues/connected-via-email
- https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/nonces-and-api-wallets
- https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/activation-gas-fee
- https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/exchange-endpoint

---

### 4.2 Aster

**概述与入门：**

- Aster 有官方 `wallet login` 指南和单独的官方 `email login` 指南。
- 钱包登录指南是直接的钱包认证，但即使该路径也是链特定的，并指出在 BNB Chain 上连接时有最低 `0.001 BNB` 要求。
- 邮箱流程创建一个与邮箱绑定的 `new blockchain address`，充值需要 `USDT on Arbitrum`。
- 两条不同的私有/集成路径：
  - `Aster Pro API`：自助 API key 用于直接账户交易（每账户最多 30 个 API key）
  - `Aster Code`：独立的 `Builder + Agent/API Wallet` 委托模型，需要 100 ASTER 构建者质押

**API 认证：**

- Pro API：`X-MBX-APIKEY` 头部 + HMAC SHA256 签名
- Aster Code：每用户代理签名者密钥，通过 `/fapi/v3/` 端点的构建者审批链

**实时探测结果：**

- `GET https://fapi.asterdex.com/fapi/v1/ping` 返回 `{}`。
- `GET https://fapi.asterdex.com/fapi/v1/time` 返回实时 `serverTime`。
- `GET https://fapi.asterdex.com/fapi/v1/exchangeInfo` 返回完整的实时市场/工具模式。
- `HEAD https://www.asterdex.com/en/api-management` 返回 `200`。
- Aster Code 私有探测序列：

```json
{
  "base_url": "https://fapi.asterdex.com",
  "probe_sequence": [
    {
      "endpoint": "/fapi/v3/agent",
      "query": "timestamp=1&recvWindow=5000",
      "status_code": 400,
      "json": {"code": -1102, "msg": "Mandatory parameter 'nonce' was not sent, was empty/null, or malformed."}
    },
    {
      "endpoint": "/fapi/v3/agent",
      "query": "timestamp=1&recvWindow=5000&nonce=1",
      "status_code": 400,
      "json": {"code": -1102, "msg": "Mandatory parameter 'user' was not sent, was empty/null, or malformed."}
    },
    {
      "endpoint": "/fapi/v3/agent",
      "query": "timestamp=1&recvWindow=5000&nonce=1&user=0x41Deec4e76e46c18719E90920C1efef370B7DB0A",
      "status_code": 400,
      "json": {"code": -1102, "msg": "Mandatory parameter 'signature' was not sent, was empty/null, or malformed."}
    }
  ]
}
```

**第二轮修正：**

- Aster 不应仅被建模为 `Aster Code only`。Pro API 路径是标准的自助服务，且实质上更简单。
- 双轨制：直接交易者路径（HMAC）vs 委托平台路径（构建者 + 代理签名者）。

**评估：**

- `Web2 友好度`：高
- `API 下单可用性`：对直接交易者高（Pro API key）；对构建者集成中等（Aster Code）
- `关键摩擦`：新钱包仍需入门、充值和 UI 端 API key 创建

**来源：**

- https://docs.asterdex.com/product/help/how-to-login-to-aster-with-your-wallet
- https://docs.asterdex.com/product/help/how-to-login-to-aster-with-your-email-address
- https://docs.asterdex.com/product/aster-perpetuals/api
- https://docs.asterdex.com/product/aster-perpetuals/api/how-to-create-an-api
- https://www.asterdex.com/en/api-management
- https://asterdex.github.io/aster-api-website/futures/general-info/
- https://asterdex.github.io/aster-api-website/asterCode/integration-flow/

---

### 4.3 trade[XYZ]

**概述与入门：**

- trade[XYZ] 提供两条清晰的入门路径：`Privy email wallet` 或 `connect existing wallet`。
- 文档明确说用户可以 `create a new wallet directly in our interface via Privy`，将其定位为新用户的 `fastest option`。
- 现有钱包用户必须验证钱包控制权，确保在 `Hyperliquid` 网络上，然后 `Deposit to Get Started`，之后可能还需要点击 `Enable Trading`。
- 通过 trade[XYZ] 访问的所有市场都在 `Hyperliquid` 上运行。`XYZ protocol` 是一个 `HIP-3 DEX instance`。
- trade[XYZ] 是访问 XYZ 市场的界面，且 `is not the exclusive means of accessing XYZ markets`。

**API 认证：**

- 直接使用 Hyperliquid API：主网为 `https://api.hyperliquid.xyz`，测试网为 `https://api.hyperliquid-testnet.xyz`。
- 对于 XYZ 特定市场，传递 `type: perpDexs` 和工具标识符如 `xyz:XYZ100`。

**实时探测结果：**

- `HEAD https://trade.xyz` 返回 `200`。
- `HEAD https://app.trade.xyz` 返回 `200`。
- 生产环境 CSP 信号：
  - `child-src` / `frame-src` 包含 `https://auth.privy.io`
  - `connect-src` 包含 `https://*.hyperliquid.xyz` 和 `https://*.hyperliquid-testnet.xyz`
  - WebSocket 访问 `wss://*.hyperliquid.xyz` 和 `wss://*.hyperliquid-testnet.xyz`
- HTML 包含指向 `https://api-ui.hyperliquid.xyz` 的 `preconnect` 和 `dns-prefetch` 提示。

**评估：**

- `Web2 友好度`：高
- `API 下单可用性`：中高（因为底层是 Hyperliquid）
- `关键摩擦`：Privy 简化了入门，但连接钱包用户仍面临充值、Enable Trading、独立余额桶和地理限制

**来源：**

- https://docs.trade.xyz/about-trade-xyz/introduction
- https://docs.trade.xyz/getting-started/creating-or-connecting-your-wallet
- https://docs.trade.xyz/api/overview
- https://docs.trade.xyz/about-trade-xyz/hyperliquid-xyz-and-hip-3
- https://docs.trade.xyz/support-and-faqs/faqs/trade-xyz-and-equities-xyz-markets

---

### 4.4 EdgeX

**概述与入门：**

- EdgeX 支持 `wallet login` 和 `MPC Login Users`（通过邮箱）。
- 邮箱登录在邮箱验证后为用户创建一个 `EVM address`。
- 实时前端现在特别加载了一个 `lib-privy` bundle，CSP 中有 `auth.privy.io`。

**API 认证：**

- 双层认证栈：
  1. 私有 REST：`X-edgeX-Api-Key` + `X-edgeX-Passphrase` + `X-edgeX-Signature` + `X-edgeX-Timestamp`
  2. 下单签名：独立的 `L2 private key` + `l2Signature`，使用 Pedersen 哈希 + ECDSA

**实时探测结果：**

```json
{
  "public_funding_rate": {
    "status": 200,
    "body": {"code": "SUCCESS", "data": []}
  },
  "private_get_position_transaction_page": {
    "no_headers": {"code": "GATEWAY_HEADER_REQUIRED", "msg": "header required : 'X-edgeX-Api-Key'"},
    "api_key_only": {"code": "GATEWAY_HEADER_REQUIRED", "msg": "header required : 'X-edgeX-Passphrase'"},
    "api_key_and_passphrase": {"code": "GATEWAY_HEADER_REQUIRED", "msg": "header required : 'X-edgeX-Signature'"},
    "fake_full_header_stack": {"code": "INVALID_ACCOUNT_ID", "msg": "invalid accountId : 543429922991899150"}
  },
  "private_register_account": {
    "no_headers": {"code": "GATEWAY_HEADER_REQUIRED", "msg": "header required : 'X-edgeX-Api-Key'"}
  }
}
```

**第二轮修正：**

- 文档/运行时不匹配：文档说 `X-edgeX-Api-Timestamp` 和 `X-edgeX-Api-Signature`；实时网关要求 `X-edgeX-Api-Key`、`X-edgeX-Passphrase`、`X-edgeX-Signature` 和 `X-edgeX-Timestamp`。
- 前端确实是 Privy 支持的，而不仅仅是抽象的邮箱功能。

**评估：**

- `Web2 友好度`：前端高，集成层中等
- `API 下单可用性`：对独立集成者中低
- `关键摩擦`：高；交易所原生私有 API 凭据叠加在独立的 L2 签名路径之上

**来源：**

- https://edgex-1.gitbook.io/edgeX-documentation/getting-started/accounts-and-wallets
- https://edgex-1.gitbook.io/edgeX-documentation/api/authentication
- https://edgex-1.gitbook.io/edgeX-documentation/api/private-api/account-api
- https://edgex-1.gitbook.io/edgeX-documentation/api/private-api/order-api
- https://edgex-1.gitbook.io/edgeX-documentation/api/sign

---

### 4.5 Variational

**概述与入门：**

- 分为 `Omni`（已上线）和 `Pro`（未上线）。
- Omni 是 `first live app on the Variational Protocol`，专注于永续合约交易。
- Pro `currently not live`（仅候补名单）。
- 充值是 `wallet-first`、`gasless`，使用 EIP-712/EIP-2612，固定 0.1 USDC 费用。

**API 认证：**

- 当前上线的 API 是只读的 Omni 统计数据。
- 交易 API 明确表示"still in development, and is not yet available to any users."
- 旧版 Pro API 文档（HMAC/API-key）返回 404，旧主机名无法解析。

**实时探测结果：**

```json
{
  "stats_endpoint": {"url": "https://omni-client-api.prod.ap-northeast-1.variational.io/metadata/stats", "status": 200},
  "web_apps": {
    "omni_mainnet": {"url": "https://omni.variational.io", "status": 200},
    "omni_testnet": {"url": "https://omni.testnet.variational.io", "status": 200}
  },
  "dns_resolution": {
    "pro.testnet.variational.io": {"error": "[Errno 8] nodename nor servname provided, or not known"},
    "api.testnet.variational.io": {"error": "[Errno 8] nodename nor servname provided, or not known"},
    "api.variational.io": {"error": "[Errno 8] nodename nor servname provided, or not known"}
  }
}
```

**评估：**

- `Web2 友好度`：充值 UX 中等，账户抽象低
- `API 下单可用性`：目前实际上不可用
- `关键摩擦`：阻碍因素在于上线的产品/API 界面仅为只读

**来源：**

- https://docs.variational.io/
- https://docs.variational.io/technical-documentation/api
- https://docs.variational.io/pro/about-pro

---

### 4.6 Lighter

**概述与入门：**

- 钱包优先 + 先充值 + API 原生。
- 用户需要一个 `Ethereum wallet` 来注册。实时应用显示 `Connect Wallet to Trade`。
- 未发现邮箱、社交登录或嵌入式钱包标记。

**API 认证：**

- 主账户 + 子账户
- API key 公钥/私钥对（索引 2-254 用于程序化使用，0-1 保留给网页/移动端）
- 每密钥 nonce 管理 + 认证令牌生成（最长 8 小时规范型，1 天-10 年只读型）
- Standard 与 Premium 账户类型

**实时探测结果：**

```json
{
  "order_book_details": {"status": 200},
  "accounts_by_l1_address": {"body": {"code": 21100, "message": "account not found"}},
  "create_intent_address": {"body": {"code": 200, "intent_address": "0x59B28B2c5414da224f32146895b95cCD283D75e9"}},
  "next_nonce": {
    "account_index_0_api_key_2": {"body": {"code": 200, "nonce": 0}},
    "account_index_999999999_api_key_2": {"body": {"code": 200, "nonce": 0}}
  }
}
```

**评估：**

- `Web2 友好度`：低
- `API 下单可用性`：账户创建后高
- `关键摩擦`：充值 -> 入账 -> account_index -> API key 关联 -> 认证令牌 -> 签名交易

**来源：**

- https://docs.lighter.xyz/trading/api
- https://apidocs.lighter.xyz/docs/get-started
- https://apidocs.lighter.xyz/docs/create-accounts-programmatically
- https://apidocs.lighter.xyz/docs/api-keys

---

### 4.7 GRVT

**概述与入门：**

- 样本中最明确的 `Web2 + Web3 拆分`。
- Web2 登录：邮箱/密码 + Google + Microsoft + Apple OAuth。
- Web3 凭据：`SecureKey`（外部钱包或由 Dfns + Privy 提供支持的 GRVT Native SecureKey）。
- 初始化链：创建 Web2 账户 -> KYC -> SecureKey -> Web3 账户 -> 充值 -> API Key -> 签名者标签。
- 企业账户注册已启用。
- KYC 现已成为必需（`NEW_KYC_ENABLE=1`）。

**API 认证：**

- API key 登录或 EIP-712 钱包登录，两者都返回会话 cookie。
- 每个 Funding Account 最多 5 个密钥，每个 Trading Account 最多 5 个。
- 每个密钥绑定到一个 Ethereum 地址（不是 SecureKey 地址）。
- IP 白名单可用（最多 5 个 IP）。

**实时探测结果：**

```json
{
  "testnet_wallet_login": {
    "endpoint": "https://edge.testnet.grvt.io/auth/wallet/login",
    "status": 401,
    "body": "{\"status\":401,\"message\":\"wallet address not registered\"}"
  },
  "mainnet_wallet_login": {
    "endpoint": "https://edge.grvt.io/auth/wallet/login",
    "status": 400,
    "body": "{\"status\":400,\"message\":\"invalid wallet address\"}"
  },
  "testnet_market_data": {
    "endpoint": "https://market-data.testnet.grvt.io/full/v1/instrument",
    "status": 200,
    "sample_instrument": "BTC_USDT_Perp"
  },
  "mainnet_all_instruments": {
    "endpoint": "https://market-data.grvt.io/full/v1/all_instruments",
    "instrument_count": 95
  }
}
```

**测试网环境配置提取：**

```json
{
  "NEXT_PUBLIC_PRIVY_APP_ID": "cm8ftdmx602461229vprun741",
  "NEXT_PUBLIC_DFNS_ENABLE": "1",
  "NEXT_PUBLIC_DFNS_PRIVY_ENABLE": "1",
  "NEXT_PUBLIC_DFNS_APP_ID": "ap-3u16a-jq5e8-9gaqa965d49p5u36",
  "NEXT_PUBLIC_APPLE_OAUTH_ENABLE": "1",
  "NEXT_PUBLIC_MSFT_SIGN_UP_OAUTH_REQUIRED": "1",
  "NEXT_PUBLIC_NEW_KYC_ENABLE": "1",
  "NEXT_PUBLIC_ENABLE_BUSINESS_ACCOUNT_SIGNUP": "1",
  "NEXT_PUBLIC_MFA_TOTP_FORCED_FLAGS": "111111111",
  "NEXT_PUBLIC_GRVT_CHAIN_ID": "326"
}
```

**第二轮修正：**

- Native SecureKey 是 Dfns + Privy 组合体，而非仅 Privy。
- Apple 和 Microsoft OAuth 均已启用（最广泛的 Web2 社交登录）。
- KYC 新确认为必需。
- MFA/TOTP 似乎被强制启用。
- 拥有自己的 L2 链（Chain ID 326 测试网 / 325 主网）。

**评估：**

- `Web2 友好度`：非常高（样本中最广泛）
- `从冷钱包 API 下单可用性`：仍然低
- `关键摩擦`：KYC + 冷钱包返回 401 + 钱包注册现在需要邮箱 + Native SecureKey 无法接收充值 + MFA 强制

**来源：**

- https://help.grvt.io/en/articles/13038840-how-to-log-in-sign-up-with-your-wallet-step-by-step-guide
- https://help.grvt.io/en/articles/10085106-what-is-a-grvt-native-securekey
- https://api-docs.grvt.io/
- https://api-docs.grvt.io/api_setup/
- https://docs.tealstreet.io/docs/connect/grvt
- https://github.com/wezzcoetzee/grvt

---

### 4.8 Extended

**概述与入门：**

- 钱包优先，基础设施密集。账户创建需要两次签名 + 注册。
- 账户和签名密钥对存储在浏览器本地。
- 每个钱包最多 10 个交易账户。
- 测试网在 Sepolia 上：每个钱包每天可领取 $100,000 测试 USDC。
- Dynamic.xyz 存在于 JS bundle 中（不是 Privy）。

**API 认证：**

- 只读：仅 API key（`X-Api-Key` 头部）
- 写入：API key + Stark 签名（SNIP12 / EIP-712 for Starknet）
- 速率限制：默认层级 1000 请求/分钟

**实时探测结果：**

在测试网上完成了完整的写路径验证：
1. 入门完成：account_id 16233，状态 ACTIVE
2. API key 创建成功：32 字符密钥
3. 余额：1000 USD 可用于交易
4. 签名 IOC 订单被接受：
   - 市场：BTC-USD，方向：BUY，数量：0.0001，价格：35618.6
   - 订单结果：status=CANCELLED，reason=NO_LIQUIDITY
5. 探测后无挂单

主网：121 个市场，BTC-USD 显示约 $339M 日交易量，$106M 未平仓量。

**第二轮修正：**

- Dynamic.xyz 钱包 SDK 确实存在于 JS bundle 中（第一轮遗漏了）。
- Rhino.fi 跨链桥接已确认用于 Arbitrum 充值。
- 121 个主网市场（此前暗示规模更小）。
- USDC 是唯一抵押品（非 USDT）。
- SDK `drop-api-keys` 分支暗示未来认证简化。

**评估：**

- `Web2 友好度`：低中等（Dynamic.xyz 存在但未展示）
- `API 下单可用性`：对专业集成者非常高（完整写路径已验证）
- `关键摩擦`：Stark key 推导较复杂但可完全脚本化端到端

**来源：**

- https://api.docs.extended.exchange/
- https://docs.extended.exchange/extended-resources/account-operations/account-creation
- https://github.com/x10xchange/python_sdk

---

### 4.9 Paradex

**概述与入门：**

- 不再仅限于 `wallet-only`：当前文档支持通过 SIWE 直接 EVM 入门 + Privy 邮箱/社交钱包。
- EVM 钱包从 EVM 地址确定性地推导 Paradex 账户。
- 测试网账户创建后自动充值 $1,000,000 测试 USDC。

**API 认证：**

- JWT 令牌用于认证访问
- 主私钥用于完全访问
- 子密钥用于仅交易权限
- 只读令牌用于长期 GET 访问
- EVM 认证 v2 使用 SIWE（`personal_sign`）

**实时探测结果：**

- 主网 CSP `frame-src`：`https://login.argent.xyz`、`https://login.ready.co`、`https://privy.paradex.trade`
- 测试网 CSP `frame-src`：`https://privy.testnet.paradex.trade`
- 测试网配置返回了实时 Starknet 参数。
- 本地探测推导了确定性 L2 地址：`0x6e0b56155cbf1e6e2762134da12f47ca62d003e1934a3469bdc3dedf6bb2cb3`
- `POST /v1/onboarding` 返回：`INSUFFICIENT_MIN_CHAIN_BALANCE`（需要 Ethereum/Arbitrum/Base 上 0.001 ETH 或 5 USDC）
- `POST /v1/auth` 返回：`NOT_ONBOARDED`

**第二轮修正：**

- Privy 邮箱/社交路径是真实的（在 CSP 和文档中已确认）。
- 但冷程序化路径仍受外部链上余额要求限制。
- 官方 SDK/代码示例仍倾向于旧版 L2 私钥流程。

**评估：**

- `Web2 友好度`：中高
- `API 下单可用性`：非常高
- `关键摩擦`：账户存在前需要最低外部链上余额；文档/运行时与 SDK 示例未完全对齐

**来源：**

- https://docs.paradex.trade/docs/accounts/wallet-overview
- https://docs.paradex.trade/docs/testnet-faqs
- https://docs.paradex.trade/api/general-information/api-authentication
- https://github.com/tradeparadex/code-samples
- https://github.com/tradeparadex/paradex-py

---

### 4.10 Ethereal

**概述与入门：**

- 文档/API 中钱包优先，但 CSP 揭示了 Privy + Fun.xyz + MoonPay 集成。
- 所有交易通过子账户；首次充值创建子账户。
- 默认子账户名 `primary`（bytes32 编码）。
- 链接签名者用于 UI/一键交易；90 天后过期；每 7 天最多 5 个。

**API 认证：**

- EIP-712 逐操作签名，8 种不同类型
- 无 JWT 或 API key 替代方案
- 49 个 REST 端点 + 原生 WebSocket v2

**实时探测结果：**

```json
{
  "probe_date": "2026-03-21",
  "api_host": "https://api.ethereal.trade",
  "wallet": "0x41Deec4e76e46c18719E90920C1efef370B7DB0A",
  "product": {"status": 200, "sample_display_tickers": ["BTC-USD", "ETH-USD", "SOL-USD"]},
  "subaccount": {"status": 200, "body": {"data": [], "hasNext": false}},
  "linked_signer": {
    "status": 404,
    "body": {"message": "Signer not found: 0x41deec4e76e46c18719e90920c1efef370b7db0a", "error": "Not Found", "statusCode": 404}
  }
}
```

**CSP 分析：**

- `frame-src` 中的 `auth.privy.io`（Privy）
- `connect-src` 中的 `*.fun.xyz`，HTML 中的 `funkit`（Fun.xyz）
- `connect-src` 中的 `static.moonpay.com`、`api.moonpay.com`（MoonPay 法币）
- `api.relay.link`（Relay 跨链桥接）
- `api.merkl.xyz`（Merkl 奖励）
- `dna.zerion.io`（Zerion 钱包）
- `wallet.binance.com`（Binance Wallet）
- `mainnet.zklighter.elliot.ai`（ZK Lighter 交叉引用）
- `api.statsig.com`（功能标志）

**第二轮修正：**

- 嵌入式钱包基础设施确实存在（Privy、Fun.xyz、MoonPay）—— 第一轮结论是错误的。
- 测试网访问更受限（仅 Discord，无公共水龙头）。
- API 界面：49 个端点（比第一轮捕获的更多）。
- WebSocket 升级到原生 v2。
- 15 个主网 / 17 个测试网产品。

**评估：**

- `Web2 友好度`：中等（Privy/Fun.xyz 在 CSP 中但不在开发者文档中）
- `从冷钱包 API 下单可用性`：仍然低
- `关键摩擦`：无 API key 认证，首次充值必需，测试网仅 Discord 可用，链接签名者有速率限制，仅支持 USDe 充值

**来源：**

- https://docs.ethereal.trade/trading/perpetual-futures/ethereal-testnet
- https://docs.ethereal.trade/developer-guides/trading-api/quick-start
- https://docs.ethereal.trade/developer-guides/trading-api/accounts-and-signers
- https://docs.ethereal.trade/developer-guides/trading-api/message-signing
- https://docs.ethereal.trade/developer-guides/trading-api/websocket-gateway

---

### 4.11 Nado

**概述与入门：**

- 最明确地教导 `CEX users how to think in DEX terms`。
- 无 API key，无用户名/密码；认证通过钱包签名。
- 子账户默认名 `default`；由首次充值创建（最低 $5 USDT0）。
- 链接签名者 = `1-Click Trading`（产品化的委托，每个子账户一个）。
- 链接签名者可以做主钱包能做的任何事，包括提款。

**API 认证：**

- 纯钱包签名（写操作 EIP-712，读取查询无需签名）
- 无 API key 抽象
- 链接签名者可选

**实时探测结果：**

```json
{
  "wallet": "0x41Deec4e76e46c18719E90920C1efef370B7DB0A",
  "default_subaccount": "0x41deec4e76e46c18719e90920c1efef370b7db0a64656661756c740000000000",
  "gateway_with_compression": {
    "status": {"status": "success", "data": "active"},
    "subaccount_info": {"exists": false, "spot_count": 5, "perp_count": 43},
    "linked_signer": {"linked_signer": "0x0000000000000000000000000000000000000000"}
  },
  "archive": {
    "subaccounts": [],
    "direct_deposit_address": {"v1_address": "0x740a7e70262c143bb88d901001d141f986deda45"}
  }
}
```

**第二轮修正：**

- 链接签名者流程比裸委托密钥原语更精致（产品化为 `1-Click Trading`）。
- Gateway 需要压缩头部（`Accept-Encoding: gzip/br/deflate`）。
- 子账户存在之前即可查询充值地址。

**评估：**

- `Web2 友好度`：入门时低，使用 1-Click Trading 充值后中等
- `API 下单可用性`：对熟悉钱包签名的机器人中等
- `关键摩擦`：无 API key 抽象；首次充值是硬性前提

**来源：**

- https://docs.nado.xyz/developer-resources/get-started/core-concepts
- https://docs.nado.xyz/developer-resources/get-started/quickstart
- https://docs.nado.xyz/developer-resources/get-started/first-deposit
- https://docs.nado.xyz/developer-resources/get-started/linked-signers
- https://docs.nado.xyz/developer-resources/api/endpoints

---

### 4.12 StandX

**概述与入门：**

- 运行在 `BNB Chain (BSC)` 和 `Solana` 上。团队：前 `Binance Futures` 创始团队 + `Goldman Sachs` 校友。
- 仅钱包登录（Connect Wallet）。无邮箱/社交登录。无嵌入式钱包信号。
- 使用专有的生息稳定币 `DUSD` 作为保证金。
- 双钱包系统：Cash Wallet（充值持有）+ Perps Wallet（活跃交易）。
- 正确域名：`standx.com`（不是 `standx.trade`）。

**API 认证：**

- 通过钱包签名获取 JWT（SIWE + ed25519 密钥对）—— 无传统 API key
- 流程：生成 ed25519 密钥对 -> prepare-signin -> 签名 SIWE 消息 -> 登录 -> JWT（7-180 天）
- 写操作需要 Body 签名（ed25519）
- 自托管令牌管理：可配置权限（Trade、Withdraw）、过期时间、可撤销

**实时探测结果：**

```json
{
  "api_status": {"endpoint": "https://api.standx.com", "status": 200, "body": "{\"time\":1774100678301,\"status\":\"ok\"}"},
  "symbol_info": {"status": 200, "markets": ["BTC-USD", "ETH-USD", "XAG-USD", "XAU-USD"]},
  "btc_market": {"last_price": "70701", "volume_24h": "4230.74 BTC", "volume_quote_24h": "$297M DUSD", "open_interest": "317.54 BTC ($22.4M)"},
  "prepare_signin": {"status": 200, "note": "Returns valid signedData JWT for test wallet. SIWE message with chainId=56 (BSC)."},
  "private_no_auth": {"status": 401, "body": "{\"code\":401,\"message\":\"missing jwt\"}"},
  "geo": {"region": "TH"}
}
```

**评估：**

- `Web2 友好度`：低（仅钱包登录）
- `API 下单可用性`：高（干净的 REST + WebSocket，可与 CEX API 相比）
- `关键摩擦`：必须持有 DUSD，双钱包转账步骤，写操作需要 ed25519 Body 签名，仅 4 个市场
- `独特之处`：DUSD 在交易时产生 APY 收益，双链 BSC+Solana 统一 API

**来源：**

- https://docs.standx.com
- https://docs.standx.com/standx-api/standx-api
- https://docs.standx.com/standx-api/perps-http
- https://docs.standx.com/standx-api/perps-ws
- https://docs.standx.com/standx-api/perps-auth

---

### 4.13 MYX Finance

**概述与入门：**

- 多链永续合约 DEX，部署在 `BNB Chain`、`Arbitrum`、`Linea`、`opBNB` 上。
- 使用 `Matching Pool Mechanism (MPM)` 实现零滑点。
- USDC 保证金，最高 50 倍杠杆，37 个交易对。
- V2 由 `Consensys` 资助（2026 年 2 月）—— 模块化衍生品结算引擎，尚未上线。
- 双路径入门：钱包连接或通过 `Particle Network` 的社交登录（邮箱、手机、社交）。
- `Seamless Key`：专有的委托交易密钥，通过 Biconomy 中继器免 gas，非托管。
- 新用户流程：社交登录 -> 信用卡购买 USDC -> 创建 Seamless Key。

**API 认证：**

- 仅 2 个公共 REST 端点（市场数据，无认证）。
- **无链下 REST 交易 API。** 所有交易通过智能合约（Router.sol）在链上进行。
- 无 WebSocket API。无官方 SDK。

**实时探测结果：**

```json
{
  "api_contracts": {"endpoint": "https://api.myx.finance/v2/quote/market/contracts", "status": 200, "count": 37},
  "api_btc_spec": {"endpoint": "https://api.myx.finance/v2/quote/market/contract_specs?ticker_id=BTC-USDC", "status": 200, "body": {"contract_type": "Vanilla", "contract_price_currency": "USDC"}}
}
```

**JS Bundle 信号：**

- Particle Network（社交登录/嵌入式钱包）
- Biconomy（账户抽象/无 gas）
- WalletConnect、ConnectKit、RainbowKit
- Dynamic.xyz、Magic.link、Safe
- EIP-4337、EIP-7702 引用

**评估：**

- `Web2 友好度`：高（Particle Network + Seamless Key + 信用卡）
- `API 下单可用性`：低（无链下交易 API；仅智能合约）
- `关键摩擦`：无 REST 下单 API，智能合约集成需要 Solidity 知识，无 SDK

**来源：**

- https://myxfinance.gitbook.io/myx
- https://myxfinance.gitbook.io/myx/protocol/api
- https://myxfinance.gitbook.io/myx/trading-mechanism/introducing-myx-seamless-trading
- https://github.com/myx-protocol/myx-contracts

---

## 第五部分：代码测试记录

### 5.1 测试脚本

#### 5.1.1 hyperliquid_safe_probe.py -- Hyperliquid 测试网 SDK 探测（noop + 市价单）

```python
#!/usr/bin/env python3.11

import json
import re
import sys
import time
from pathlib import Path


ROOT = Path(__file__).resolve().parent
SDK_PATH = ROOT / ".pydeps_hl"
DEFAULT_WALLET_FILE = ROOT / "evm_wallet_20260320_165220.txt"

ADDRESS_PATTERN = re.compile(r"^Address:\s*(0x[a-fA-F0-9]{40})$")
PRIVATE_KEY_PATTERN = re.compile(r"^Private Key:\s*(0x[a-fA-F0-9]{64})$")

sys.path.insert(0, str(SDK_PATH))

from eth_account import Account  # noqa: E402
from hyperliquid.exchange import Exchange  # noqa: E402
from hyperliquid.utils import constants  # noqa: E402
import requests  # noqa: E402


def load_wallet(wallet_file: Path) -> tuple[str, str]:
    address = None
    private_key = None

    for raw_line in wallet_file.read_text(encoding="utf-8").splitlines():
        line = raw_line.strip()
        address_match = ADDRESS_PATTERN.match(line)
        if address_match:
            address = address_match.group(1)
            continue

        private_key_match = PRIVATE_KEY_PATTERN.match(line)
        if private_key_match:
            private_key = private_key_match.group(1)

    if not address or not private_key:
        raise ValueError(f"Could not parse wallet file: {wallet_file}")

    return address, private_key


def try_call(fn):
    try:
        return {"ok": True, "result": fn()}
    except Exception as exc:  # noqa: BLE001
        return {
            "ok": False,
            "error_type": type(exc).__name__,
            "error": str(exc),
        }


def post_info(payload):
    response = requests.post(
        f"{constants.TESTNET_API_URL}/info",
        json=payload,
        timeout=30,
        headers={"Content-Type": "application/json", "User-Agent": "codex-hl-probe"},
    )
    return {
        "status_code": response.status_code,
        "json": response.json(),
    }


def main() -> int:
    address, private_key = load_wallet(DEFAULT_WALLET_FILE)
    wallet = Account.from_key(private_key)

    meta = post_info({"type": "meta"})["json"]
    exchange = Exchange(
        wallet,
        base_url=constants.TESTNET_API_URL,
        meta=meta,
        spot_meta={"universe": [], "tokens": []},
    )

    results = {
        "wallet_address": address,
        "base_url": constants.TESTNET_API_URL,
        "meta_probe": {
            "ok": True,
            "universe_count": len(meta.get("universe", [])),
        },
        "pre_state": try_call(lambda: post_info({"type": "clearinghouseState", "user": address})),
        "pre_open_orders": try_call(lambda: post_info({"type": "openOrders", "user": address})),
    }

    nonce = int(time.time() * 1000)
    results["noop"] = try_call(lambda: exchange.noop(nonce))

    # Intentionally use an IOC market-style order on a zero-balance wallet.
    # If auth/signing works, the expected outcome is a clean trading-layer rejection
    # such as insufficient margin, not an auth-format failure.
    results["market_open_btc_buy_0_001"] = try_call(lambda: exchange.market_open("BTC", True, 0.001, slippage=0.01))

    print(json.dumps(results, indent=2, ensure_ascii=False))
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
```

#### 5.1.2 extended_onboarding_probe.py -- Extended 测试网入门（账户创建 + API key）

```python
#!/usr/bin/env python3.11

import asyncio
import json
import re
import sys
from pathlib import Path


ROOT = Path(__file__).resolve().parent
SDK_PATH = ROOT / ".pydeps311"
DEFAULT_WALLET_FILE = ROOT / "evm_wallet_20260320_165220.txt"

ADDRESS_PATTERN = re.compile(r"^Address:\s*(0x[a-fA-F0-9]{40})$")
PRIVATE_KEY_PATTERN = re.compile(r"^Private Key:\s*(0x[a-fA-F0-9]{64})$")

sys.path.insert(0, str(SDK_PATH))

from x10.perpetual.configuration import TESTNET_CONFIG  # noqa: E402
from x10.perpetual.trading_client.account_module import AccountModule  # noqa: E402
from x10.perpetual.user_client.user_client import UserClient  # noqa: E402


def load_wallet(wallet_file: Path) -> tuple[str, str]:
    address = None
    private_key = None

    for raw_line in wallet_file.read_text(encoding="utf-8").splitlines():
        line = raw_line.strip()
        address_match = ADDRESS_PATTERN.match(line)
        if address_match:
            address = address_match.group(1)
            continue

        private_key_match = PRIVATE_KEY_PATTERN.match(line)
        if private_key_match:
            private_key = private_key_match.group(1)

    if not address or not private_key:
        raise ValueError(f"Could not parse wallet file: {wallet_file}")

    return address, private_key


def redact_api_key(api_key: str) -> dict:
    if len(api_key) <= 8:
        return {"prefix": api_key, "length": len(api_key)}
    return {"prefix": api_key[:8], "suffix": api_key[-4:], "length": len(api_key)}


async def run_probe(wallet_file: Path) -> dict:
    address, private_key = load_wallet(wallet_file)
    client = UserClient(TESTNET_CONFIG, lambda: private_key)
    results = {
        "wallet_address": address,
        "testnet_host": TESTNET_CONFIG.onboarding_url,
        "api_base_url": TESTNET_CONFIG.api_base_url,
    }

    onboarded_account = None
    fetched_accounts = []

    try:
        onboarded = await client.onboard()
        onboarded_account = onboarded.account
        results["onboard"] = {
            "ok": True,
            "account": onboarded.account.model_dump(mode="json"),
            "l2_public_key": onboarded.l2_key_pair.public_hex,
        }
    except Exception as exc:  # noqa: BLE001
        results["onboard"] = {
            "ok": False,
            "error_type": type(exc).__name__,
            "error": str(exc),
        }

    try:
        accounts = await client.get_accounts()
        fetched_accounts = accounts
        results["get_accounts"] = {
            "ok": True,
            "count": len(accounts),
            "accounts": [
                {
                    "account": account.account.model_dump(mode="json"),
                    "l2_public_key": account.l2_key_pair.public_hex,
                }
                for account in accounts
            ],
        }
    except Exception as exc:  # noqa: BLE001
        results["get_accounts"] = {
            "ok": False,
            "error_type": type(exc).__name__,
            "error": str(exc),
        }

    target_account = None
    if onboarded_account is not None:
        target_account = onboarded_account
    elif fetched_accounts:
        target_account = fetched_accounts[0].account

    if target_account is not None:
        try:
            api_key = await client.create_account_api_key(target_account, description="codex probe")
            results["create_account_api_key"] = {
                "ok": True,
                "api_key": redact_api_key(api_key),
                "account_id": target_account.id,
            }
        except Exception as exc:  # noqa: BLE001
            results["create_account_api_key"] = {
                "ok": False,
                "error_type": type(exc).__name__,
                "error": str(exc),
                "account_id": target_account.id,
            }
    else:
        results["create_account_api_key"] = {
            "ok": False,
            "error_type": "NoAccountAvailable",
            "error": "No onboarded account was available for API key creation.",
        }

    if results["create_account_api_key"].get("ok"):
        account_module = AccountModule(TESTNET_CONFIG, api_key=api_key)
        try:
            account_response = await account_module.get_account()
            account_data = account_response.data
            results["private_get_account"] = {
                "ok": True,
                "account": account_data.model_dump(mode="json") if account_data is not None else None,
            }
        except Exception as exc:  # noqa: BLE001
            results["private_get_account"] = {
                "ok": False,
                "error_type": type(exc).__name__,
                "error": str(exc),
            }

        try:
            orders_response = await account_module.get_open_orders()
            orders = orders_response.data or []
            results["private_get_open_orders"] = {
                "ok": True,
                "count": len(orders),
            }
        except Exception as exc:  # noqa: BLE001
            results["private_get_open_orders"] = {
                "ok": False,
                "error_type": type(exc).__name__,
                "error": str(exc),
            }

        await account_module.close_session()

    await client.close_session()
    return results


def main() -> int:
    wallet_file = DEFAULT_WALLET_FILE
    results = asyncio.run(run_probe(wallet_file))
    print(json.dumps(results, indent=2, ensure_ascii=False))
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
```

#### 5.1.3 extended_write_probe.py -- Extended 测试网写路径（签名 IOC 订单）

```python
#!/usr/bin/env python3.11

import asyncio
import json
import re
import sys
from decimal import Decimal, ROUND_DOWN, ROUND_UP
from pathlib import Path


ROOT = Path(__file__).resolve().parent
SDK_PATH = ROOT / ".pydeps311"
DEFAULT_WALLET_FILE = ROOT / "evm_wallet_20260320_165220.txt"

ADDRESS_PATTERN = re.compile(r"^Address:\s*(0x[a-fA-F0-9]{40})$")
PRIVATE_KEY_PATTERN = re.compile(r"^Private Key:\s*(0x[a-fA-F0-9]{64})$")

sys.path.insert(0, str(SDK_PATH))

from x10.perpetual.accounts import StarkPerpetualAccount  # noqa: E402
from x10.perpetual.configuration import TESTNET_CONFIG  # noqa: E402
from x10.perpetual.markets import MarketModel  # noqa: E402
from x10.perpetual.orders import OrderSide, OrderStatus, TimeInForce  # noqa: E402
from x10.perpetual.trading_client.trading_client import PerpetualTradingClient  # noqa: E402
from x10.perpetual.user_client.user_client import UserClient  # noqa: E402


def load_wallet(wallet_file: Path) -> tuple[str, str]:
    address = None
    private_key = None

    for raw_line in wallet_file.read_text(encoding="utf-8").splitlines():
        line = raw_line.strip()
        address_match = ADDRESS_PATTERN.match(line)
        if address_match:
            address = address_match.group(1)
            continue

        private_key_match = PRIVATE_KEY_PATTERN.match(line)
        if private_key_match:
            private_key = private_key_match.group(1)

    if not address or not private_key:
        raise ValueError(f"Could not parse wallet file: {wallet_file}")

    return address, private_key


def redact_api_key(api_key: str) -> dict:
    if len(api_key) <= 8:
        return {"prefix": api_key, "length": len(api_key)}
    return {"prefix": api_key[:8], "suffix": api_key[-4:], "length": len(api_key)}


def pick_market(markets: list[MarketModel]) -> MarketModel:
    active_markets = [
        market
        for market in markets
        if market.active and market.market_stats.bid_price > 0 and market.market_stats.ask_price > 0
    ]
    if not active_markets:
        raise ValueError("No active markets with non-zero bid/ask were returned.")

    for keyword in ("BTC", "ETH", "SOL"):
        for market in active_markets:
            if keyword in market.name.upper():
                return market

    return active_markets[0]


def build_safe_ioc_order(market: MarketModel) -> dict:
    bid = market.market_stats.bid_price
    ask = market.market_stats.ask_price
    tick = market.trading_config.min_price_change
    floor = market.trading_config.limit_price_floor
    cap = market.trading_config.limit_price_cap

    buy_candidate = max(floor, min(bid * Decimal("0.5"), ask - tick))
    buy_price = market.trading_config.round_price(buy_candidate, rounding_direction=ROUND_DOWN)
    if buy_price < ask:
        return {
            "side": OrderSide.BUY,
            "price": buy_price,
            "strategy": "IOC buy placed well below best ask to avoid execution and resting.",
        }

    sell_candidate = min(cap, max(ask * Decimal("1.5"), bid + tick))
    sell_price = market.trading_config.round_price(sell_candidate, rounding_direction=ROUND_UP)
    if sell_price > bid:
        return {
            "side": OrderSide.SELL,
            "price": sell_price,
            "strategy": "IOC sell placed well above best bid to avoid execution and resting.",
        }

    raise ValueError(f"Could not build a non-crossing IOC order for market {market.name}.")


async def fetch_order_state(client: PerpetualTradingClient, order_id: int, retries: int = 5) -> dict:
    last_error = None
    for attempt in range(1, retries + 1):
        try:
            response = await client.account.get_order_by_id(order_id)
            return {
                "ok": True,
                "attempt": attempt,
                "order": response.data.model_dump(mode="json") if response.data is not None else None,
            }
        except Exception as exc:  # noqa: BLE001
            last_error = {
                "attempt": attempt,
                "error_type": type(exc).__name__,
                "error": str(exc),
            }
            await asyncio.sleep(1)

    return {
        "ok": False,
        "error": last_error,
    }


async def run_probe(wallet_file: Path) -> dict:
    address, private_key = load_wallet(wallet_file)
    user_client = UserClient(TESTNET_CONFIG, lambda: private_key)
    trading_client = None

    results = {
        "wallet_address": address,
        "testnet_host": TESTNET_CONFIG.onboarding_url,
        "api_base_url": TESTNET_CONFIG.api_base_url,
    }

    try:
        accounts = await user_client.get_accounts()
        if not accounts:
            raise ValueError("No accounts returned by Extended testnet onboarding API.")

        onboarded_account = accounts[0]
        api_key = await user_client.create_account_api_key(
            onboarded_account.account,
            description="codex write probe",
        )

        results["account"] = {
            "id": onboarded_account.account.id,
            "account_index": onboarded_account.account.account_index,
            "status": onboarded_account.account.status,
            "l2_vault": onboarded_account.account.l2_vault,
            "l2_public_key": onboarded_account.l2_key_pair.public_hex,
        }
        results["api_key"] = redact_api_key(api_key)

        stark_account = StarkPerpetualAccount(
            vault=onboarded_account.account.l2_vault,
            private_key=onboarded_account.l2_key_pair.private_hex,
            public_key=onboarded_account.l2_key_pair.public_hex,
            api_key=api_key,
        )
        trading_client = PerpetualTradingClient(TESTNET_CONFIG, stark_account=stark_account)

        try:
            balance_before = await trading_client.account.get_balance()
            results["balance_before_claim"] = {
                "ok": True,
                "balance": balance_before.data.model_dump(mode="json") if balance_before.data is not None else None,
            }
        except Exception as exc:  # noqa: BLE001
            results["balance_before_claim"] = {
                "ok": False,
                "error_type": type(exc).__name__,
                "error": str(exc),
            }

        try:
            claim = await trading_client.testnet.claim_testing_funds()
            results["claim_testing_funds"] = {
                "ok": True,
                "response": claim.data.model_dump(mode="json") if claim.data is not None else None,
            }
            await asyncio.sleep(1)
        except Exception as exc:  # noqa: BLE001
            results["claim_testing_funds"] = {
                "ok": False,
                "error_type": type(exc).__name__,
                "error": str(exc),
            }

        try:
            balance_after_claim = await trading_client.account.get_balance()
            results["balance_after_claim"] = {
                "ok": True,
                "balance": balance_after_claim.data.model_dump(mode="json") if balance_after_claim.data is not None else None,
            }
        except Exception as exc:  # noqa: BLE001
            results["balance_after_claim"] = {
                "ok": False,
                "error_type": type(exc).__name__,
                "error": str(exc),
            }

        try:
            client_info = await trading_client.account.get_client()
            results["client_info_after_claim"] = {
                "ok": True,
                "client": client_info.data.model_dump(mode="json") if client_info.data is not None else None,
            }
        except Exception as exc:  # noqa: BLE001
            results["client_info_after_claim"] = {
                "ok": False,
                "error_type": type(exc).__name__,
                "error": str(exc),
            }

        markets_response = await trading_client.markets_info.get_markets()
        markets = markets_response.data or []
        selected_market = pick_market(markets)
        order_plan = build_safe_ioc_order(selected_market)
        order_qty = selected_market.trading_config.min_order_size

        results["selected_market"] = {
            "name": selected_market.name,
            "bid_price": str(selected_market.market_stats.bid_price),
            "ask_price": str(selected_market.market_stats.ask_price),
            "mark_price": str(selected_market.market_stats.mark_price),
            "min_order_size": str(selected_market.trading_config.min_order_size),
            "min_order_size_change": str(selected_market.trading_config.min_order_size_change),
            "min_price_change": str(selected_market.trading_config.min_price_change),
            "limit_price_floor": str(selected_market.trading_config.limit_price_floor),
            "limit_price_cap": str(selected_market.trading_config.limit_price_cap),
        }
        results["planned_probe_order"] = {
            "side": order_plan["side"],
            "qty": str(order_qty),
            "price": str(order_plan["price"]),
            "time_in_force": TimeInForce.IOC,
            "strategy": order_plan["strategy"],
        }

        placed_order_response = await trading_client.place_order(
            market_name=selected_market.name,
            amount_of_synthetic=order_qty,
            price=order_plan["price"],
            side=order_plan["side"],
            time_in_force=TimeInForce.IOC,
            external_id="codex-extended-write-probe",
        )
        placed_order = placed_order_response.data
        results["place_order"] = {
            "ok": True,
            "order": placed_order.model_dump(mode="json") if placed_order is not None else None,
        }

        if placed_order is not None:
            fetched_order = await fetch_order_state(trading_client, placed_order.id)
            results["fetch_order_state"] = fetched_order

            open_orders = await trading_client.account.get_open_orders(market_names=[selected_market.name])
            open_order_list = open_orders.data or []
            results["open_orders_after_probe"] = {
                "ok": True,
                "count": len(open_order_list),
                "orders": [order.model_dump(mode="json") for order in open_order_list],
            }

            active_statuses = {OrderStatus.NEW, OrderStatus.PARTIALLY_FILLED, OrderStatus.UNTRIGGERED}
            if fetched_order.get("ok") and fetched_order.get("order") is not None:
                order_status = fetched_order["order"].get("status")
                if order_status in active_statuses:
                    try:
                        await trading_client.orders.cancel_order(placed_order.id)
                        results["cleanup_cancel"] = {
                            "ok": True,
                            "order_id": placed_order.id,
                        }
                    except Exception as exc:  # noqa: BLE001
                        results["cleanup_cancel"] = {
                            "ok": False,
                            "order_id": placed_order.id,
                            "error_type": type(exc).__name__,
                            "error": str(exc),
                        }
        return results
    except Exception as exc:  # noqa: BLE001
        results["probe_error"] = {
            "error_type": type(exc).__name__,
            "error": str(exc),
        }
        return results
    finally:
        if trading_client is not None:
            await trading_client.info.close_session()
            await trading_client.testnet.close_session()
            await trading_client.close()
        await user_client.close_session()


def main() -> int:
    results = asyncio.run(run_probe(DEFAULT_WALLET_FILE))
    print(json.dumps(results, indent=2, ensure_ascii=False))
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
```

#### 5.1.4 grvt_wallet_login_test.mjs -- GRVT 测试网 EIP-712 钱包登录

```javascript
import fs from "node:fs";
import { Wallet, Signature } from "ethers";

const walletFile = "/Users/ocean/Documents/Perp DEX测试/evm_wallet_20260320_165220.txt";
const walletText = fs.readFileSync(walletFile, "utf8");
const privateKeyMatch = walletText.match(/Private Key:\s*(0x[a-fA-F0-9]{64})/);

if (!privateKeyMatch) {
  throw new Error("Private key not found in wallet file");
}

const privateKey = privateKeyMatch[1];
const wallet = new Wallet(privateKey);

const serverTimeRes = await fetch("https://market-data.testnet.grvt.io/time");
if (!serverTimeRes.ok) {
  throw new Error(`Failed to fetch GRVT server time: ${serverTimeRes.status}`);
}

const serverTimeJson = await serverTimeRes.json();
const serverTimeMs = BigInt(serverTimeJson.server_time);
const expiration = (serverTimeMs * 1_000_000n) + (5n * 60n * 1_000_000_000n);
const nonce = Math.floor(Math.random() * 0xffffffff);

const domain = {
  name: "GRVT Exchange",
  version: "0",
  chainId: 326,
};

const types = {
  WalletLogin: [
    { name: "signer", type: "address" },
    { name: "nonce", type: "uint32" },
    { name: "expiration", type: "int64" },
  ],
};

const value = {
  signer: wallet.address,
  nonce,
  expiration,
};

const rawSignature = await wallet.signTypedData(domain, types, value);
const sig = Signature.from(rawSignature);

const loginRes = await fetch("https://edge.testnet.grvt.io/auth/wallet/login", {
  method: "POST",
  headers: {
    "content-type": "application/json",
    "cookie": "rm=true;",
  },
  body: JSON.stringify({
    address: wallet.address,
    signature: {
      signer: wallet.address,
      v: sig.v,
      r: sig.r,
      s: sig.s,
      nonce,
      expiration: expiration.toString(),
      chain_id: "326",
    },
  }),
});

const responseText = await loginRes.text();

console.log(
  JSON.stringify(
    {
      address: wallet.address,
      status: loginRes.status,
      ok: loginRes.ok,
      setCookie: loginRes.headers.get("set-cookie"),
      body: responseText,
    },
    null,
    2,
  ),
);
```

#### 5.1.5 paradex_wallet_probe.mjs -- Paradex L2 密钥推导 + 入门/认证载荷生成

```javascript
import fs from 'fs';

import { ethers } from 'ethers';
import { keyDerivation } from '@starkware-industries/starkware-crypto-utils';
import * as Starknet from 'starknet';

const WALLET_FILE =
  '/Users/ocean/Documents/Perp DEX测试/evm_wallet_20260320_165220.txt';
const CONFIG_FILE =
  '/Users/ocean/Documents/Perp DEX测试/paradex_testnet_config.json';
function normalizeHex(value) {
  if (typeof value === 'string') {
    return value.startsWith('0x') ? value : `0x${value}`;
  }
  return `0x${BigInt(value).toString(16)}`;
}

function readWalletPrivateKey() {
  const txt = fs.readFileSync(WALLET_FILE, 'utf8');
  const match = txt.match(/Private Key:\s*(0x[0-9a-fA-F]+)/);
  if (!match) {
    throw new Error('private key not found in wallet file');
  }
  return match[1];
}

function readConfig() {
  return JSON.parse(fs.readFileSync(CONFIG_FILE, 'utf8'));
}

function chainIdHexFromString(value) {
  return `0x${Buffer.from(value, 'utf8').toString('hex')}`;
}

function formatStarkSignature(signature) {
  if (Array.isArray(signature)) {
    return [String(signature[0]), String(signature[1])];
  }
  if (signature && typeof signature === 'object' && 'r' in signature && 's' in signature) {
    return [String(signature.r), String(signature.s)];
  }
  throw new Error(`unexpected stark signature shape: ${JSON.stringify(signature)}`);
}

function buildStarkKeyTypedData(l1ChainId) {
  return {
    domain: {
      name: 'Paradex',
      version: '1',
      chainId: String(l1ChainId),
    },
    primaryType: 'Constant',
    types: {
      Constant: [{ name: 'action', type: 'string' }],
    },
    message: {
      action: 'STARK Key',
    },
  };
}

function buildOnboardingTypedData(starknetChainId) {
  return {
    message: {
      action: 'Onboarding',
    },
    domain: {
      name: 'Paradex',
      chainId: chainIdHexFromString(starknetChainId),
      version: '1',
    },
    primaryType: 'Constant',
    types: {
      StarkNetDomain: [
        { name: 'name', type: 'felt' },
        { name: 'chainId', type: 'felt' },
        { name: 'version', type: 'felt' },
      ],
      Constant: [{ name: 'action', type: 'felt' }],
    },
  };
}

function buildAuthTypedData(starknetChainId, timestamp, expiration) {
  return {
    message: {
      method: 'POST',
      path: '/v1/auth',
      body: '',
      timestamp,
      expiration,
    },
    domain: {
      name: 'Paradex',
      chainId: chainIdHexFromString(starknetChainId),
      version: '1',
    },
    primaryType: 'Request',
    types: {
      StarkNetDomain: [
        { name: 'name', type: 'felt' },
        { name: 'chainId', type: 'felt' },
        { name: 'version', type: 'felt' },
      ],
      Request: [
        { name: 'method', type: 'felt' },
        { name: 'path', type: 'felt' },
        { name: 'body', type: 'felt' },
        { name: 'timestamp', type: 'felt' },
        { name: 'expiration', type: 'felt' },
      ],
    },
  };
}

async function main() {
  const config = readConfig();
  const evmPrivateKey = readWalletPrivateKey();
  const evmWallet = new ethers.Wallet(evmPrivateKey);

  const starkKeyTypedData = buildStarkKeyTypedData(config.l1_chain_id);
  const seedSignature = await evmWallet.signTypedData(
    starkKeyTypedData.domain,
    starkKeyTypedData.types,
    starkKeyTypedData.message,
  );
  const repeatSignature = await evmWallet.signTypedData(
    starkKeyTypedData.domain,
    starkKeyTypedData.types,
    starkKeyTypedData.message,
  );

  if (seedSignature !== repeatSignature) {
    throw new Error('non-deterministic STARK key signature from EVM wallet');
  }

  const l2PrivateKey = normalizeHex(
    keyDerivation.getPrivateKeyFromEthSignature(seedSignature),
  );
  const l2PublicKey = normalizeHex(
    keyDerivation.privateToStarkKey(l2PrivateKey),
  );

  const constructorCalldata = Starknet.CallData.compile({
    implementation: config.paraclear_account_hash,
    selector: Starknet.hash.getSelectorFromName('initialize'),
    calldata: Starknet.CallData.compile({
      signer: l2PublicKey,
      guardian: '0',
    }),
  });

  const l2Address = normalizeHex(
    Starknet.hash.calculateContractAddressFromHash(
      l2PublicKey,
      config.paraclear_account_proxy_hash,
      constructorCalldata,
      0,
    ),
  );

  const provider = new Starknet.RpcProvider({
    nodeUrl: config.starknet_fullnode_rpc_url,
    chainId: Starknet.shortString.encodeShortString(config.starknet_chain_id),
  });
  const account = new Starknet.Account({
    provider,
    address: l2Address,
    signer: l2PrivateKey,
  });

  const onboardingSig = formatStarkSignature(
    await account.signMessage(buildOnboardingTypedData(config.starknet_chain_id)),
  );
  const timestamp = Math.floor(Date.now() / 1000);
  const expiration = timestamp + 24 * 60 * 60;
  const authSig = formatStarkSignature(
    await account.signMessage(
      buildAuthTypedData(config.starknet_chain_id, timestamp, expiration),
    ),
  );

  const result = {
    evmAddress: evmWallet.address,
    l2Address,
    l2PublicKey,
    onboardingRequest: {
      headers: {
        'PARADEX-ETHEREUM-ACCOUNT': evmWallet.address,
        'PARADEX-STARKNET-ACCOUNT': l2Address,
        'PARADEX-STARKNET-SIGNATURE': JSON.stringify(onboardingSig),
      },
      body: {
        public_key: l2PublicKey,
      },
    },
    authRequest: {
      headers: {
        'PARADEX-STARKNET-ACCOUNT': l2Address,
        'PARADEX-STARKNET-SIGNATURE': JSON.stringify(authSig),
        'PARADEX-TIMESTAMP': String(timestamp),
        'PARADEX-SIGNATURE-EXPIRATION': String(expiration),
      },
      timestamp,
      expiration,
    },
  };

  console.log(JSON.stringify(result, null, 2));
}

main().catch((error) => {
  console.error(error);
  process.exit(1);
});
```

---

### 5.2 测试结果

#### 5.2.1 hyperliquid_safe_probe_result.json

```json
{
  "wallet_address": "0x41Deec4e76e46c18719E90920C1efef370B7DB0A",
  "base_url": "https://api.hyperliquid-testnet.xyz",
  "meta_probe": {
    "ok": true,
    "universe_count": 207
  },
  "pre_state": {
    "ok": true,
    "result": {
      "status_code": 200,
      "json": {
        "marginSummary": {
          "accountValue": "0.0",
          "totalNtlPos": "0.0",
          "totalRawUsd": "0.0",
          "totalMarginUsed": "0.0"
        },
        "crossMarginSummary": {
          "accountValue": "0.0",
          "totalNtlPos": "0.0",
          "totalRawUsd": "0.0",
          "totalMarginUsed": "0.0"
        },
        "crossMaintenanceMarginUsed": "0.0",
        "withdrawable": "0.0",
        "assetPositions": [],
        "time": 1774069382046
      }
    }
  },
  "pre_open_orders": {
    "ok": true,
    "result": {
      "status_code": 200,
      "json": []
    }
  },
  "noop": {
    "ok": true,
    "result": {
      "status": "err",
      "response": "User or API Wallet 0x41deec4e76e46c18719e90920c1efef370b7db0a does not exist."
    }
  },
  "market_open_btc_buy_0_001": {
    "ok": true,
    "result": {
      "status": "err",
      "response": "User or API Wallet 0x41deec4e76e46c18719e90920c1efef370b7db0a does not exist."
    }
  }
}
```

#### 5.2.2 extended_onboarding_result.json

```json
{
  "wallet_address": "0x41Deec4e76e46c18719E90920C1efef370B7DB0A",
  "testnet_host": "https://api.starknet.sepolia.extended.exchange",
  "api_base_url": "https://api.starknet.sepolia.extended.exchange/api/v1",
  "onboard": {
    "ok": true,
    "account": {
      "id": 16233,
      "description": "Main account",
      "account_index": 0,
      "status": "ACTIVE",
      "l2_key": "0x51477b6e58c98344ded9a8e307479e195a796e49c3940f95cb21b2913d7a585",
      "l2_vault": 513234,
      "bridge_starknet_address": "0x067779645d55f0fd4461e918b9ceba156ed42ce52fafcf72e472964b696a2b94"
    },
    "l2_public_key": "0x51477b6e58c98344ded9a8e307479e195a796e49c3940f95cb21b2913d7a585"
  },
  "get_accounts": {
    "ok": true,
    "count": 1,
    "accounts": [
      {
        "account": {
          "id": 16233,
          "description": "Main account",
          "account_index": 0,
          "status": "ACTIVE",
          "l2_key": "0x51477b6e58c98344ded9a8e307479e195a796e49c3940f95cb21b2913d7a585",
          "l2_vault": 513234,
          "bridge_starknet_address": "0x067779645d55f0fd4461e918b9ceba156ed42ce52fafcf72e472964b696a2b94"
        },
        "l2_public_key": "0x51477b6e58c98344ded9a8e307479e195a796e49c3940f95cb21b2913d7a585"
      }
    ]
  },
  "create_account_api_key": {
    "ok": true,
    "api_key": {
      "prefix": "4bd4dd51",
      "suffix": "a5a9",
      "length": 32
    },
    "account_id": 16233
  },
  "private_get_account": {
    "ok": true,
    "account": {
      "id": 16233,
      "description": "Main account",
      "account_index": 0,
      "status": "ACTIVE",
      "l2_key": "0x51477b6e58c98344ded9a8e307479e195a796e49c3940f95cb21b2913d7a585",
      "l2_vault": 513234,
      "bridge_starknet_address": "0x067779645d55f0fd4461e918b9ceba156ed42ce52fafcf72e472964b696a2b94"
    }
  },
  "private_get_open_orders": {
    "ok": true,
    "count": 0
  }
}
```

#### 5.2.3 extended_write_probe_result.json

```json
{
  "wallet_address": "0x41Deec4e76e46c18719E90920C1efef370B7DB0A",
  "testnet_host": "https://api.starknet.sepolia.extended.exchange",
  "api_base_url": "https://api.starknet.sepolia.extended.exchange/api/v1",
  "account": {
    "id": 16233,
    "account_index": 0,
    "status": "ACTIVE",
    "l2_vault": 513234,
    "l2_public_key": "0x51477b6e58c98344ded9a8e307479e195a796e49c3940f95cb21b2913d7a585"
  },
  "api_key": {
    "prefix": "62b1f847",
    "suffix": "3598",
    "length": 32
  },
  "balance_before_claim": {
    "ok": true,
    "balance": {
      "collateral_name": "USD",
      "balance": "1000",
      "equity": "1000",
      "available_for_trade": "1000",
      "available_for_withdrawal": "1000",
      "unrealised_pnl": "0",
      "initial_margin": "0",
      "margin_ratio": "0.0000",
      "updated_time": 1774072275028
    }
  },
  "claim_testing_funds": {
    "ok": true,
    "response": {
      "id": 2035232950446071808
    }
  },
  "balance_after_claim": {
    "ok": true,
    "balance": {
      "collateral_name": "USD",
      "balance": "1000",
      "equity": "1000",
      "available_for_trade": "1000",
      "available_for_withdrawal": "1000",
      "unrealised_pnl": "0",
      "initial_margin": "0",
      "margin_ratio": "0.0000",
      "updated_time": 1774072275028
    }
  },
  "client_info_after_claim": {
    "ok": true,
    "client": {
      "id": 17059,
      "evm_wallet_address": "0x41deec4e76e46c18719e90920c1efef370b7db0a",
      "starknet_wallet_address": null,
      "referral_link_code": null
    }
  },
  "selected_market": {
    "name": "BTC-USD",
    "bid_price": "71237.3",
    "ask_price": "74400",
    "mark_price": "70650.277546750003",
    "min_order_size": "0.0001",
    "min_order_size_change": "0.00001",
    "min_price_change": "0.1",
    "limit_price_floor": "0.05",
    "limit_price_cap": "0.05"
  },
  "planned_probe_order": {
    "side": "BUY",
    "qty": "0.0001",
    "price": "35618.6",
    "time_in_force": "IOC",
    "strategy": "IOC buy placed well below best ask to avoid execution and resting."
  },
  "place_order": {
    "ok": true,
    "order": {
      "id": 2035232962947313664,
      "external_id": "codex-extended-write-probe"
    }
  },
  "fetch_order_state": {
    "ok": true,
    "attempt": 2,
    "order": {
      "id": 2035232962947313664,
      "account_id": 16233,
      "external_id": "codex-extended-write-probe",
      "market": "BTC-USD",
      "type": "LIMIT",
      "side": "BUY",
      "status": "CANCELLED",
      "status_reason": "NO_LIQUIDITY",
      "price": "35618.6000000000000000",
      "average_price": null,
      "qty": "0.0001000000000000",
      "filled_qty": "0E-16",
      "cancelled_qty": "0E-16",
      "reduce_only": false,
      "post_only": false,
      "payed_fee": "0E-16",
      "created_time": 1774072325820,
      "updated_time": 1774072325874,
      "expiry_time": null,
      "time_in_force": "IOC",
      "tp_sl_type": null,
      "take_profit": null,
      "stop_loss": null
    }
  },
  "open_orders_after_probe": {
    "ok": true,
    "count": 0,
    "orders": []
  }
}
```

#### 5.2.4 grvt_wallet_login_result.json

```json
{
  "address": "0x41Deec4e76e46c18719E90920C1efef370B7DB0A",
  "status": 401,
  "ok": false,
  "setCookie": null,
  "body": "{\"status\":401,\"message\":\"wallet address not registered\"}"
}
```

#### 5.2.5 ethereal_public_probe_result.json

```json
{
  "probe_date": "2026-03-21",
  "api_host": "https://api.ethereal.trade",
  "wallet": "0x41Deec4e76e46c18719E90920C1efef370B7DB0A",
  "product": {
    "status": 200,
    "sample_display_tickers": [
      "BTC-USD",
      "ETH-USD",
      "SOL-USD"
    ]
  },
  "subaccount": {
    "status": 200,
    "body": {
      "data": [],
      "hasNext": false
    }
  },
  "linked_signer": {
    "status": 404,
    "body": {
      "message": "Signer not found: 0x41deec4e76e46c18719e90920c1efef370b7db0a",
      "error": "Not Found",
      "statusCode": 404
    }
  }
}
```

---

### 5.3 基于 curl 的探测摘要

**StandX：**
- `POST /v1/offchain/prepare-signin?chain=bsc` 使用测试钱包地址返回了一个有效的 `signedData` JWT，包含 chainId=56 的 SIWE 消息。
- `GET /api/query_symbol_info` 返回了 4 个市场（BTC-USD、ETH-USD、XAG-USD、XAU-USD）。
- `GET /api/query_balance` 无认证返回 `{"code":401,"message":"missing jwt"}`。
- `GET /api/query_symbol_market?symbol=BTC-USD` 返回：last_price $70,701，volume_24h $297M DUSD。

**MYX：**
- `GET /v2/quote/market/contracts` 返回了跨 BNB Chain、Arbitrum、Linea 的 37 个永续合约交易对。
- `GET /v2/quote/market/contract_specs?ticker_id=BTC-USDC` 返回：contract_type Vanilla，currency USDC。
- `app.myx.finance` CSP 非常简洁（仅 `upgrade-insecure-requests`）—— 所有供应商信号都在 JS bundle 中。

**GRVT 测试网环境配置提取**（来自 `https://testnet.grvt.io/api/env`）：
- `NEXT_PUBLIC_PRIVY_APP_ID`: `cm8ftdmx602461229vprun741`
- `NEXT_PUBLIC_DFNS_ENABLE`: `1`
- `NEXT_PUBLIC_DFNS_PRIVY_ENABLE`: `1`
- `NEXT_PUBLIC_DFNS_APP_ID`: `ap-3u16a-jq5e8-9gaqa965d49p5u36`
- `NEXT_PUBLIC_DFNS_ORG_ID`: `or-1r3dt-7kcb9-0r8ckn81rag7lit`
- `NEXT_PUBLIC_APPLE_OAUTH_ENABLE`: `1`
- `NEXT_PUBLIC_MSFT_SIGN_UP_OAUTH_REQUIRED`: `1`
- `NEXT_PUBLIC_NEW_KYC_ENABLE`: `1`
- `NEXT_PUBLIC_GRVT_CHAIN_ID`: `326`
- `NEXT_PUBLIC_MFA_TOTP_FORCED_FLAGS`: `111111111`

**Ethereal CSP 头部提取**（来自 `app.ethereal.trade`）：
- `frame-src`：`auth.privy.io`（Privy）
- `connect-src`：`*.fun.xyz`、`wss://*.fun.xyz`（Fun.xyz 钱包 SDK）
- `connect-src`：`static.moonpay.com`、`api.moonpay.com`（MoonPay 法币）
- `connect-src`：`api.relay.link`（Relay 桥接）、`api.merkl.xyz`（Merkl 奖励）、`dna.zerion.io`（Zerion）、`wallet.binance.com`（Binance Wallet）、`mainnet.zklighter.elliot.ai`（ZK Lighter）
- `connect-src`：`api.statsig.com`（功能标志）
- HTML：存在 `funkit` 字符串（Fun.xyz SDK）

**Extended JS bundle 分析**（来自 `/assets/index-CvzKfSsU.js`，1.77MB）：
- `dynamicEnvironmentId: d4582476-d6fe-4ddb-bc66-2c0941e05456`（Dynamic.xyz）
- `rhinoApiKey: PUBLIC-981a60fc-93a6-4cba-adbf-5ef9960a459b`、`rhinoBaseUrl: https://api.rhino.fi`（Rhino.fi 桥接）
- 10+ WalletConnect 引用
- 0 个 Privy 引用（未使用 Privy）
- 构建版本：`app-exchange-v0-64-18-1fc8c2f90`、`runtimeEnv: starknet-prod`

---

## 第六部分：第二轮结果文件

所有 13 个平台均有第二轮结果 JSON 文件：

| 文件 | 平台 | 说明 |
|---|---|---|
| `hyperliquid_second_pass_result.json` | Hyperliquid | 文档复查 + 测试网重新探测。确认 Privy 邮箱路径、激活门控。 |
| `aster_second_pass_result.json` | Aster | 双 API 轨道修正（Pro API vs Aster Code）。 |
| `tradexyz_second_pass_result.json` | trade[XYZ] | CSP 确认 Privy + Hyperliquid 运行时。 |
| `edgex_second_pass_result.json` | EdgeX | 网关头部序列探测 + CSP 中的 Privy。 |
| `variational_probe_result.json` | Variational | 旧版主机的 DNS 解析失败。仅只读 API。 |
| `lighter_second_pass_result.json` | Lighter | 文档复查 + createIntentAddress 探测。确认先充值。 |
| `grvt_second_pass_result.json` | GRVT | 环境配置提取（Dfns+Privy、KYC、OAuth）。最广泛的 Web2。 |
| `extended_second_pass_result.json` | Extended | JS bundle 中的 Dynamic.xyz，121 个主网市场，Rhino.fi 桥接。 |
| `paradex_second_pass_result.json` | Paradex | CSP 中的 Privy，最低链上余额门控，L2 密钥推导探测。 |
| `ethereal_second_pass_result.json` | Ethereal | CSP 揭示 Privy+Fun.xyz+MoonPay。49 个端点。WebSocket v2。 |
| `nado_second_pass_result.json` | Nado | 1-Click Trading 产品化。压缩头部要求。 |
| `standx_second_pass_result.json` | StandX | 完整 API 界面记录。JWT 认证流程。4 个市场。 |
| `myx_second_pass_result.json` | MYX Finance | Particle+Biconomy+Dynamic+Magic。仅链上。V2 由 Consensys 支持。 |

此外，第一轮探测结果：

| 文件 | 平台 | 说明 |
|---|---|---|
| `hyperliquid_safe_probe_result.json` | Hyperliquid | SDK 探测：noop + 市价单均被拒绝。 |
| `extended_onboarding_result.json` | Extended | 测试网入门 + API key 创建成功。 |
| `extended_write_probe_result.json` | Extended | 完整的签名 IOC 订单已下达并解析。 |
| `grvt_wallet_login_result.json` | GRVT | EIP-712 钱包登录返回 401。 |
| `ethereal_public_probe_result.json` | Ethereal | 空子账户 + 404 签名者。 |
| `aster_private_probe_result.json` | Aster | Aster Code 私有端点探测序列。 |
| `edgex_probe_result.json` | EdgeX | 网关头部要求序列。 |
| `lighter_probe_result.json` | Lighter | 账户未找到 + 意向地址已创建。 |
| `nado_probe_result.json` | Nado | Gateway 状态 + 子账户 + 充值地址。 |
| `paradex_probe_payload.json` | Paradex | 推导的 L2 地址 + 入门/认证载荷。 |
| `paradex_testnet_config.json` | Paradex | 实时测试网系统配置（Starknet 参数）。 |

---

## 第七部分：更新排名（全部 13 个平台）

### 最佳 Web2 用户转化

1. **MYX Finance** —— Particle Network 社交登录 + Seamless Key 无 gas 交易 + 信用卡购买 USDC。从零加密到交易的最顺畅路径，但仅通过链上智能合约（无 REST API）。
2. **GRVT** —— Dfns+Privy Native SecureKey + 邮箱 + Google + Apple + Microsoft OAuth + KYC。最广泛的 Web2 社交登录提供商，但 API 访问的初始化链最复杂。
3. **trade[XYZ]** —— Privy 邮箱钱包被描述为新用户的"fastest option"。前端流畅，但底层继承了 Hyperliquid 的激活约束。
4. **Aster** —— 官方邮箱登录指南，邮箱创建新的区块链地址。双轨 Pro API + Aster Code。
5. **EdgeX** —— Privy 支持的 MPC 邮箱登录。交易所原生凭据栈之上的 Web2 友好入口。
6. **Hyperliquid** —— Privy 邮箱登录带钱包导出。仍需 Enable Trading。

### 最佳 API 优先 / 专业交易者

1. **Extended** —— 完整的写路径端到端验证（入门 -> API key -> 签名 IOC 订单）。121 个主网市场，$339M 日 BTC 交易量。认证栈较重（EVM + Stark + API key）但完全可脚本化。
2. **StandX** —— 干净的 REST + WebSocket 交易 API，可与 CEX API 相比。通过钱包签名的 JWT 认证，有完善的 EVM + Solana 示例文档。仅 4 个市场。
3. **Paradex** —— JWT + 子密钥 + 只读令牌。最清晰的凭据委托模型。测试网 $1M 测试 USDC。但冷钱包仍受最低链上余额限制。
4. **Hyperliquid** —— 代理钱包模式有清晰的文档和广泛采用。激活后干净，但需先激活/充值。
5. **Lighter** —— 成熟的 API key + 认证令牌 + 子账户栈。Standard 与 Premium 层级。但需先充值且无邮箱登录。
6. **Ethereal** —— 49 个 REST 端点 + 原生 WebSocket v2。丰富的 API 界面。但 EIP-712 逐操作（无 API key），且测试网访问现在仅限 Discord。

### 无 REST 交易 API 可用

1. **MYX Finance** —— 仅链上（智能合约调用或 Seamless Key）。37 个交易对但无链下 REST 下单 API。带账户抽象的 V2 尚未上线。
2. **Variational** —— 交易 API 明确表示"仍在开发中"。当前上线的 API 是只读的 Omni 统计数据。Pro 未上线。旧版 API 主机无法解析。

### 首次 API 下单前初始化最繁重

1. **GRVT** —— Web2 账户 -> KYC -> SecureKey -> Trading Account -> 充值 -> API Key -> 签名者标签（7 个步骤）。
2. **EdgeX** —— API Key + Passphrase + Signature + Timestamp + 独立 L2 密钥 + l2Signature。
3. **Ethereal** —— Discord 测试网请求 -> 充值 -> 子账户 -> EIP-712 逐操作 -> 链接签名者有速率限制。
4. **Lighter** —— 充值 -> 入账 -> account_index -> API key 关联 -> 认证令牌。
5. **Nado** —— 首次充值 -> 子账户 -> 每个操作都需钱包签名。

---

## 第八部分：附录

### 测试钱包

- **地址：** `0x41Deec4e76e46c18719E90920C1efef370B7DB0A`
- **创建时间：** 2026-03-20
- **资金：** 所有链上零真实资金。仅在 Extended（Sepolia）上有测试网活动。
- **注意：** 私钥未包含在本报告中。本地存储在 `evm_wallet_20260320_165220.txt` 中。

### 测试的 URL

**Hyperliquid：**
- `https://api.hyperliquid.xyz/info`（主网）
- `https://api.hyperliquid-testnet.xyz/info`（测试网）

**Aster：**
- `https://fapi.asterdex.com/fapi/v1/ping`
- `https://fapi.asterdex.com/fapi/v1/time`
- `https://fapi.asterdex.com/fapi/v1/exchangeInfo`
- `https://fapi.asterdex.com/fapi/v3/agent`
- `https://fapi.asterdex.com/fapi/v3/builder`
- `https://www.asterdex.com/en/api-management`

**trade[XYZ]：**
- `https://trade.xyz`
- `https://app.trade.xyz`

**EdgeX：**
- `https://pro.edgex.exchange`
- `https://pro.edgex.exchange/api/v1/public/funding/getLatestFundingRate`
- `https://pro.edgex.exchange/api/v1/private/account/getPositionTransactionPage`
- `https://pro.edgex.exchange/api/v1/private/account/registerAccount`

**Variational：**
- `https://omni.variational.io`
- `https://omni.testnet.variational.io`
- `https://omni-client-api.prod.ap-northeast-1.variational.io/metadata/stats`
- `https://docs.variational.io/technical-documentation/api`
- `https://docs.variational.io/technical-documentation/api/authentication`（404）

**Lighter：**
- `https://app.lighter.xyz`
- `https://mainnet.zklighter.elliot.ai/api/v1/orderBookDetails`
- `https://mainnet.zklighter.elliot.ai/api/v1/accountsByL1Address`
- `https://mainnet.zklighter.elliot.ai/api/v1/createIntentAddress`
- `https://mainnet.zklighter.elliot.ai/api/v1/nextNonce`
- `https://mainnet.zklighter.elliot.ai/api/v1/apikeys`
- `https://mainnet.zklighter.elliot.ai/api/v1/changeAccountTier`

**GRVT：**
- `https://edge.testnet.grvt.io/auth/wallet/login`
- `https://edge.grvt.io/auth/wallet/login`
- `https://edge.testnet.grvt.io/auth/api_key/login`
- `https://market-data.testnet.grvt.io/time`
- `https://market-data.grvt.io/time`
- `https://market-data.testnet.grvt.io/full/v1/instrument`
- `https://market-data.grvt.io/full/v1/all_instruments`
- `https://testnet.grvt.io/api/env`

**Extended：**
- `https://api.starknet.sepolia.extended.exchange/api/v1/info/markets`（测试网）
- `https://api.starknet.extended.exchange/api/v1/info/markets`（主网）
- `https://api.starknet.extended.exchange/api/v1/info/markets/BTC-USD/stats`（主网）
- `https://app.extended.exchange`（JS bundle 分析）

**Paradex：**
- `https://app.paradex.trade`
- `https://app.testnet.paradex.trade`
- `https://api.testnet.paradex.trade/v1/system/config`
- `https://api.testnet.paradex.trade/v1/onboarding`
- `https://api.testnet.paradex.trade/v1/auth`

**Ethereal：**
- `https://api.ethereal.trade/v1/product`
- `https://api.etherealtest.net/v1/product`
- `https://api.ethereal.trade/v1/subaccount`
- `https://api.ethereal.trade/v1/linked-signer/address/<wallet>`
- `https://api.ethereal.trade/v1/rpc/config`
- `https://api.ethereal.trade/openapi.json`
- `https://app.ethereal.trade`（CSP 分析）

**Nado：**
- `https://app.nado.xyz`
- `https://gateway.test.nado.xyz/v1/query`（status、subaccount_info、linked_signer、symbols）
- `https://archive.test.nado.xyz/v1`（subaccounts、direct_deposit_address）

**StandX：**
- `https://api.standx.com`
- `https://perps.standx.com/api/query_symbol_info`
- `https://perps.standx.com/api/query_symbol_market`
- `https://perps.standx.com/api/query_depth_book`
- `https://perps.standx.com/api/query_balance`
- `https://api.standx.com/v1/offchain/prepare-signin`
- `https://geo.standx.com/v1/region`

**MYX Finance：**
- `https://api.myx.finance/v2/quote/market/contracts`
- `https://api.myx.finance/v2/quote/market/contract_specs`
- `https://app.myx.finance`（JS bundle 分析）

### 工件文件列表

`/Users/ocean/Documents/Perp DEX测试/` 中的所有文件：

| 文件 | 类型 | 说明 |
|---|---|---|
| `evm_wallet_20260320_165220.txt` | 钱包 | 测试钱包（地址 + 私钥） |
| `hyperliquid_safe_probe.py` | 脚本 | Hyperliquid 测试网 SDK 探测 |
| `hyperliquid_safe_probe_result.json` | 结果 | Hyperliquid 探测输出 |
| `hyperliquid_second_pass_result.json` | 结果 | Hyperliquid 第二轮分析 |
| `extended_onboarding_probe.py` | 脚本 | Extended 测试网入门探测 |
| `extended_onboarding_result.json` | 结果 | Extended 入门输出 |
| `extended_write_probe.py` | 脚本 | Extended 测试网写路径探测 |
| `extended_write_probe_result.json` | 结果 | Extended 写路径输出 |
| `extended_second_pass_result.json` | 结果 | Extended 第二轮分析 |
| `grvt_wallet_login_test.mjs` | 脚本 | GRVT EIP-712 钱包登录探测 |
| `grvt_wallet_login_result.json` | 结果 | GRVT 登录输出 |
| `grvt_second_pass_result.json` | 结果 | GRVT 第二轮分析（环境配置） |
| `paradex_wallet_probe.mjs` | 脚本 | Paradex L2 密钥推导 + 载荷生成 |
| `paradex_probe_payload.json` | 结果 | Paradex 推导的地址 + 签名 |
| `paradex_testnet_config.json` | 结果 | Paradex 测试网系统配置 |
| `paradex_second_pass_result.json` | 结果 | Paradex 第二轮分析 |
| `ethereal_public_probe_result.json` | 结果 | Ethereal 公共 API 探测输出 |
| `ethereal_second_pass_result.json` | 结果 | Ethereal 第二轮分析（CSP） |
| `ethereal_swagger_ui_init.js` | 数据 | Ethereal OpenAPI 规范（311KB） |
| `aster_private_probe_result.json` | 结果 | Aster Code 私有端点探测 |
| `aster_second_pass_result.json` | 结果 | Aster 第二轮分析 |
| `tradexyz_second_pass_result.json` | 结果 | trade[XYZ] 第二轮分析 |
| `edgex_probe_result.json` | 结果 | EdgeX 网关头部序列 |
| `edgex_second_pass_result.json` | 结果 | EdgeX 第二轮分析 |
| `variational_probe_result.json` | 结果 | Variational DNS + API 探测 |
| `lighter_probe_result.json` | 结果 | Lighter 第一轮探测 |
| `lighter_second_pass_result.json` | 结果 | Lighter 第二轮分析 |
| `nado_probe_result.json` | 结果 | Nado gateway + archive 探测 |
| `nado_second_pass_result.json` | 结果 | Nado 第二轮分析 |
| `standx_second_pass_result.json` | 结果 | StandX 完整 API 分析 |
| `myx_second_pass_result.json` | 结果 | MYX Finance 完整分析 |
| `first_pass_platform_probe.md` | 笔记 | 第一轮探测笔记 |
| `full_perp_dex_research_20260321.md` | 报告 | 完整研究报告（本文档的来源） |
| `package.json` | 配置 | Node.js 依赖 |
| `package-lock.json` | 配置 | Node.js 锁定文件 |
| `node_modules/` | 目录 | Node.js 依赖（ethers、starknet、starkware-crypto-utils） |
| `.pydeps311/` | 目录 | Python 3.11 依赖（Extended SDK） |
| `.pydeps_hl/` | 目录 | Python 依赖（Hyperliquid SDK） |
